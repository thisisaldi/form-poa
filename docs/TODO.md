# REVISI LAYAR POA SYSTEM — TRACKER

*(Diperbarui 2026-07-27 — telah direkonsiliasi penuh dengan tracker resmi stakeholder (numbering mereka sendiri 1-80: DONE=54/ON-PROSES=19/NEED CONFIRMATION=7). Nomor pada tabel-tabel di bawah ini TETAP menggunakan numbering internal lama (1-65), bukan numbering 1-80 milik stakeholder — karena susunan/pengelompokannya berbeda. Audit kode (07-27) menemukan 8 klaim "DONE" dari stakeholder yang sebelumnya belum tercatat di sini tetapi ternyata BENAR ADA di kode (dipindahkan ke DONE di bawah), dan 6 klaim "DONE"/status lain yang TERNYATA tidak cocok dengan kode sama sekali — lihat section "⚠️ Diskrepansi" di bagian paling bawah untuk detail lengkapnya. #64 (Cancel Approved NSM), #47 (Matriks Summary Per Outlet), #48 (Matriks Summary Per Produk) dikerjakan penuh pada 07-27 — lihat catatan masing-masing di tabel DONE, termasuk catatan performa untuk #47. Sebutkan nomornya saja (numbering internal) untuk mulai mengerjakan satu item.)*

**Ringkasan**: ✅ DONE = 51 · 🟡 ON-PROSES = 3 · ❓ NEED CONFIRMATION = 8 · ⬜ BELUM DIKERJAKAN (#39-43) = 4 · ⬜ BELUM DIKERJAKAN (item baru 07-27, #49-66) = 7 *(#51/#53/#55/#65 dikerjakan penuh pada batch kedua 07-27 — lihat catatan masing-masing, termasuk trade-off performa #65. #66 "Layout Approval Drafting" ditambahkan lagi pada 07-27 — susulan ketiga dari stakeholder, belum dikerjakan. #41 dipindahkan ke DONE 07-21. #44/#45/#46 dipindahkan ke DONE 07-21. #52/#54/#56/#60/#61/#62 dipindahkan ke DONE 07-27 (low-hanging fruit). #26 (Data Survey/SFE) dipindahkan dari ON-PROSES ke DONE 07-27 — audit kode mengonfirmasi fitur sidebar-nya sudah berjalan penuh, bukan sekadar rencana. 7 item baru DONE ditambahkan 07-27 dari hasil audit kode (lihat tabel DONE, ditandai "Audit 07-27"). #36 (AP.K24 Matraman) dipindahkan ke DONE 07-27 — diperiksa langsung ke DB production, `kategori`-nya memang sudah "B". #47/#48/#64 dikerjakan penuh pada 07-27 (3 fitur besar: 2 matriks summary + Cancel Approved NSM), lihat catatan masing-masing. NEED CONFIRMATION menjadi 8 (bukan 7 seperti versi terbaru stakeholder) karena #38 (Historis Kunjungan) di-drop dari daftar mereka tanpa penjelasan (bukan DONE, bukan ON-PROSES) — dipertahankan di sini sampai ada klarifikasi. #19 tetap DONE meskipun daftar stakeholder sempat menuliskan ON-PROSES, karena sudah diverifikasi langsung di kode. #22 tetap dibiarkan ON-PROSES konsisten dengan daftar stakeholder walau kodenya sebenarnya sudah ada, karena terdapat ambiguitas scope yang belum diklarifikasi.)*

## ✅ Beres 07-31 — bug fix edit access + perf

*(Konteks: banyak fitur besar dibangun pada sesi lain di tanggal yang sama — role VIEWER, alur Ajukan Edit, perbaikan performa N+1/Seq-Scan/connection-pool, dll — sebelum sempat dibaca. Ketiganya sudah SELESAI dari sesi lain; yang dilakukan di sini hanya verifikasi ditambah perbaikan 1 bug regresi yang ditemukan saat pemeriksaan.)*

- **Bug fix: tombol Edit tidak muncul saat POA belum di-approve atasan** — `DraftChecklist.tsx` memiliki gate client-side tambahan (`editUnlocked`, harus klik "✎ Edit" terlebih dahulu untuk unlock, sudah ada sejak sebelum Lock Edit Logic #65 dibuat) yang menyebabkan Edit/Lihat per-row selalu mulai dalam keadaan terkunci, padahal Lock Edit Logic sudah menangani hal ini di server (`canEdit()` di `authz.ts`, mengembalikan `false` apabila sudah ada yang approve). Terbukti bahwa `willTriggerRevisi === true` SELALU berarti `userCanEdit === false` juga (owner terkunci di sisi server begitu ada yang approve) — sehingga cabang confirm-dialog "mengedit akan kembali ke Revisi" benar-benar UNREACHABLE, merupakan sisa gate lama yang tidak diperlukan lagi. Perbaikan: `canEditNow` disederhanakan menjadi langsung `!!userCanEdit` — tidak ada lagi klik tambahan untuk kasus belum-di-approve. Sekaligus membersihkan state/props yang sudah tidak digunakan (`editUnlocked`, `showEditConfirm`, `handleUnlockEdit`, import `ConfirmDialog`, prop `isDraft`/`willTriggerRevisi` di `DraftChecklist`/`PoaDetailTabs`/`poa/[id]/page.tsx`).
- **Ajukan Edit (setelah di-approve, wajib meminta izin edit ke approver terakhir)** — diverifikasi SUDAH lengkap dari sesi lain: `canRequestEdit`/`getLastApprover`/`canRespondEditRequest` (`authz.ts`) + `requestEditAction`/`grantEditRequestAction`/`declineEditRequestAction` (`poaWorkflow.ts`/`poa.ts`) + kartu UI "Permintaan Edit" (`poa/[id]/page.tsx`). Tidak ada perubahan, hanya dikonfirmasi berjalan sesuai desain.
- **Role VIEWER dapat mengklik "Lihat" untuk detail penuh per-dokter** — diverifikasi SUDAH lengkap: link "Lihat" pada `DraftChecklist.tsx` DoctorRow mengarah ke route yang SAMA seperti Edit (`/poa/[id]/doctor/[itemId]/edit`), dirender read-only (`readOnly={!userCanEdit}`) untuk siapa pun yang `canView` tetapi bukan `canEdit` — sehingga VIEWER/GM/SFE melihat detail LENGKAP (Histori PSSP, Kriteria Produk, seluruh field), bukan hanya ringkasan. Authorization-nya (`!userCanEdit && !canView → redirect`) sudah benar. Tidak ada perubahan.
- **Performa login/summary/struktur organisasi** — diverifikasi SUDAH diperbaiki dari sesi lain: (1) login `findFirst` case-insensitive yang memaksa Seq Scan setiap login → `findUnique` PK lookup langsung; (2) `getMrIdsUnder`/`getSubordinateIdsUnder` (struktur organisasi) dari rekursi per-node (puluhan round-trip database berurutan) → BFS per-level (≤3 query total); (3) Prisma connection pool di-hardcode dari 20 menjadi 50 (tidak lagi mengikuti default dari `DATABASE_URL` Vault yang tidak dikontrol secara harian); (4) Summary page dari mengambil SELURUH histori submission tanpa batas → dibatasi dengan window default. **Pembaruan 07-31**: window default tersebut sebelumnya rolling 4 kuartal (kuartal berjalan + 3 kuartal sebelumnya) — sesuai permintaan "dibatasi hanya untuk periode sebelumnya saja", diperketat lagi menjadi hanya **kuartal berjalan + 1 kuartal sebelumnya** (2 total). "Lihat semua periode" tetap dapat diakses 1 klik melalui filter (`SummaryFilterModal`).
- **Tombol "Lihat" (view-only) tidak terlihat jelas** — sebelumnya berupa teks abu-abu polos di `DraftChecklist.tsx` DoctorRow, mudah terlewat. Diubah menjadi pill button dengan border biru dan background tint, dengan bobot visual yang sama seperti tombol "Edit".
- **Menggabungkan section duplikat "Produk Kontes PM"** pada tab Produk Rekomendasi (form POA) — sebelumnya terdapat 2 blok dengan judul sama: satu ✓-only (`KriteriaProdukPanel`), satu lagi "...Belum Diajukan" (`ProdukKontesPanel`, hanya yang belum ditambahkan, disertai badge kriteria+PSSP). Digabungkan menjadi 1 section: seluruh produk kontes yang match, bertanda ✓ apabila sudah ditambahkan, TETAP disertai badge kriteria+PSSP. `ProdukKontesPanel` dihapus (sudah sepenuhnya superseded), `KriteriaSectionList` sekarang mendukung 2 badge sekaligus.
- **Restrukturisasi tab "Per Produk Rekomendasi" pada `/summary`** — sebelumnya 1 kartu per PAKET (6 kartu: Pencernaan/Pediatric/dst) berisi coverage bar agregat (X/Y dokter, X/Y produk per kategori). Diubah menjadi flat per KATEGORI (Produk Kontes, Low Hanging Fruit, Blue Ocean, Red Ocean, Standarisasi), masing-masing berupa tabel penuh per-produk dengan kolom yang SAMA seperti tab "Per Produk" (Estimasi Aktif+Pengajuan, Budget, Cost Ratio, Sales Aktif, dll) — bukan hanya coverage bar. `tab` (GroupingTab) sekarang me-fold "produk-rekomendasi" → "produk" (reuse penuh computation `groups` yang sudah ada), diklasifikasi ulang per kodeProduk menggunakan `OutletProductKriteria`. Komponen lama `ProdukRekomendasiView.tsx` dihapus (sepenuhnya superseded).
- **Ditemukan dan diperbaiki**: bug "Pernah PSSP" — produk yang memiliki tag "Pernah PSSP" di dropdown product picker tetapi tidak muncul di section "Pernah PSSP" tab Produk Rekomendasi. Akar masalahnya: pekerjaan konkuren pada hari yang sama sudah membuat `computePelunasanAllPeriode()` (all-time, tidak dibatasi 3 bulan) dan sudah digunakan di section "Pernah PSSP" panel, TETAPI dropdown product picker (`buildProductOptions`) masih menggunakan fungsi lama `computePelunasan3Bln()` (window 3 bulan) — sehingga 2 tempat membaca sumber data dengan window yang berbeda. Dikonfirmasi oleh user: seharusnya sekarang all-period, bukan 3 bulan lagi. Perbaikan: dropdown disamakan menggunakan `computePelunasanAllPeriode()`, fungsi lama (`computePelunasan3Bln`/`rowsActiveLast3Months`) dihapus karena sudah tidak digunakan sama sekali.

## ✅ Beres 07-21 begitu DB remote bisa diakses lagi

- ~~Jalankan `prisma migrate deploy`~~ — ternyata sudah ter-apply lebih dahulu melalui deploy pipeline (termasuk `20260720163533_grant_admin_p260054`), tetapi UPDATE-nya menjadi no-op karena User P260054 memang belum pernah ada.
- **User P260054 dibuat baru** (nama: M NAUFALDI FADHLIRRAHMAN, role ADMIN, isActive true) — sebelumnya tidak exist sama sekali di DB.
- **Hardcode sementara di `src/lib/auth.ts` (`HARDCODE_ADMIN_NIP`) sudah dihapus** — role ADMIN P260054 sekarang murni berasal dari DB.
- **NIP Anggres Saputra diperbaiki**: L240075 → **L240076** (primary key User di-rename melalui transaksi, 50 baris `MrOutletAssignment` ikut berpindah, ditambah field lain yang mungkin mereferensikan — sudah diverifikasi).

## ✅ DONE (25)

| No | Item | Catatan |
|---|---|---|
| 2 | Target sifatnya harus fix, gak bisa naik-turun | Selesai. |
| 3 | C urut historis drafting (periode aktif, tidak boleh diedit) | Direstrukturisasi 07-20 menjadi per-dokter dengan toggle Detail expand per kontrak/produk, nama disensor. Bug "tercacah" (nilai kontrak terpakai full-period, bukan diapportion ke kuartal) sudah diperbaiki. Sudah diikutsertakan ke Excel export. ⚠️ Sisa kecil: growth pelunasan 3 bulan terakhir pada section ini masih belum dikerjakan (lihat catatan #5). |
| 4 | Hari Praktek per-produk, default otomatis dari level dokter | Selesai. |
| 5 | Growth PSSP berdasarkan value 3 bulan terakhir | Kartu "Growth PSSP (3 Bln Terakhir)" di LineItemEditor, menampilkan growth dari data sales 3 bulan terakhir (terpisah dari growth berbasis kontrak PSSP lama). Ditandai selesai oleh stakeholder 07-21. ⚠️ Sisa kecil: growth **pelunasan** 3 bulan terakhir khusus di section PSSP Aktif (#3) — konsepnya mirip badge "Pernah PSSP" (#31), scope-nya masih belum dikerjakan/dikonfirmasi. |
| 6 | Nama outlet dipanjangkan di dropdown | Combobox dapat lebih lebar dari trigger-nya. Sort outlet chain-first (grup di atas, NON CHAIN di bawah) juga sudah berjalan — sempat ada bug 07-20: kolom `groupRS` di DB berisi literal string `"NON CHAIN"` (bukan `null`) untuk outlet non-chain, sehingga dianggap truthy dan tidak ter-sort; sudah diperbaiki menggunakan helper `isChainGroup()` di `LineItemEditor.tsx`. |
| 7 | Target Produk Kontes per area (target kuantitas, bukan hanya counter "1/22") | Kartu baru "Target Produk Kontes (Kuartal Ini)", menggunakan mesin `targetCalculation.ts`. Posisi di dekat tombol Export Excel. ⚠️ Menambah ~3 detik load time, belum di-cache. ⚠️ Seluruhnya bernilai 0 di data lokal (OutletSalesMonthly kosong) — perlu diperiksa di production apabila masih "belum muncul". |
| 8 | Ratio Budget/Target dihilangkan dari stat bar | Selesai — tetapi ada feedback yang meminta sebagian dikembalikan (duplikasi angka di kartu Ringkasan kanan), lihat item #9 (FEEDBACK #A.2). |
| 9 | Summary atasan perlu direview | Kemungkinan besar = FEEDBACK #A (screenshot Detail POA): (A.1) memindahkan section PSSP Aktif ke atas + stat "PSSP ESTIMASI AKTIF Q4", (A.2) menghapus duplikasi Estimasi/Target/Ratio di kartu Ringkasan kanan, (A.3) growth dari Sales Quarter sebelumnya ditampilkan kecil, (A.4) Pengali Nilai R + info visit/bulan pada baris ringkasan dokter. ⚠️ Berdasarkan cross-check 07-20: A.1/A.3/A.4 masih belum dieksekusi, hanya A.2 yang arahnya sudah jelas (resolved) tetapi belum berjalan — apabila ditandai selesai di sini, perlu diverifikasi kembali item mana yang dimaksud "sudah direview". |
| 15 | Narfoz injeksi kelipatan menjadi 5 | `konversiPembagi` 1 → 5. |
| 17 | Data diskon sinkron database | "% Diskon (DPL/DPF)" default ke `DiskonKontrak.newOnPi` asli begitu outlet+produk+Periode Awal terpilih, tetap dapat diedit manual. Apabila terdapat >1 kontrak yang match (duplikat), dipilih `newOnPi` TERBESAR. Diverifikasi end-to-end (outlet FN000441 + ACETRAM, 2 kontrak overlap 30 vs 33.5 → auto-fill 33.50). 07-20: fallback dummy "10"/"2.5" (diskon/listing fee/entertain) diganti menjadi 0 apabila memang tidak ada data kontrak sama sekali. |
| 18 | Nilai R Final per dokter di draft | Baris "Nilai R Final" (weighted avg by rencanaTotalBiaya) pada Daftar User + kolom per produk pada detail expand. ⚠️ Ambiguitas nama field "Nilai R Final" vs "Pengali Nilai R" masih belum diklarifikasi — *"yang dimasukkan di draft itu bukan nilai R akhir, melainkan pengali Nilai R"*. Jangan diubah/dihapus sebelum ada keputusan ganti-label vs tambah-field-baru. 07-20: posisi field "Pengali Nilai R" dipindahkan agar menempel langsung di bawah kartu "Nilai PSSP" (dalam kolom grid yang sama), bukan lagi full-width di bawah seluruh grid. |
| 19 | Data dosis per Produk | **Selesai 07-20** — sumber data ternyata sudah ada (`internal/List Product pharos.xlsx`, sheet "Oral Product"/"Injeksi Product"), tidak perlu menunggu ABED. Kolom "PROCOD" pada file tersebut ternyata sama persis dengan `Product.kodeProduk` (dikonfirmasi user, diverifikasi 191/192 kode match langsung ke DB). Prisma schema menambahkan 7 kolom baru (`dosisKekuatanSediaan`, `qtyPerRxPasien`, `lamaPemberianHari`, `jumlahPemberianPerHari`, `bentukSediaan`, `packing`, `indikasi`) melalui migration `20260720085421_add_product_dosis_zat_aktif_fields`, disinkronkan menggunakan script baru `scripts/syncProductZatAktifDosis.ts` (194 produk diperbarui). Ditampilkan sebagai teks abu-abu "Referensi: ..." di bawah field "Jml Produk ST / Resep" pada `LineItemEditor.tsx`. Sisa: auto-fill produk kompetitor dari data survey — belum dikerjakan. |
| 20 | Pengali Nilai R per produk, bukan totalan per dokter | Awalnya hanya memperbaiki unit `%`→`x`; versi "default dokter" dihapus total — sekarang murni per-produk. |
| 21 | Catatan tambahan "untuk memperkuat argumen pengajuan" saat Ajukan ke Atasan | Selesai. |
| 23 | Halaman approval + jumlah kunjungan MR ke user | ⚠️ **Perlu diverifikasi ulang** — berdasarkan cross-check kode 07-20 tidak ditemukan implementasinya (halaman approval yang ada belum menampilkan jumlah kunjungan MR). Ditandai selesai sesuai informasi terbaru dari stakeholder; mohon di-cross-check langsung ke halaman approval di app apabila perlu dipastikan kembali. |
| 24 | Satuan jual per box / Estimasi Produk Kontes | Kartu "Estimasi Produk Kontes" (+ stat bar atas) selesai. ⚠️ Berdasarkan cross-check 07-20: detail per-produk dengan tanda warna kategori (kontes/red ocean/low hanging fruit) pada tabel detail masih berupa tabel polos, belum ada pewarnaan di sana — apabila ini dianggap "final", berarti requirement warna kategori dianggap hanya diperlukan pada product picker (sudah ada, lihat #32), bukan pada tabel detail. |
| 28 | Summary produk belum diajukan tapi rekomendasi PM (kuning/oranye) | Terjawab melalui item #31. |
| 29 | Sidebar "Produk Kontes PM Belum Diajukan" per spesialis | Muncul di sidebar kanan form MR begitu spesialisasi dokter terdeteksi, berisi produk tier-0 yang belum ada di produkList. |
| 30 | Informasi Zat Aktif di product picker | **Selesai 07-20** — bersamaan dengan #19, dari `internal/List Product pharos.xlsx`. `Product.zatAktif` (sudah ada di schema tetapi sebelumnya selalu `null` dari `syncProducts.ts`) sekarang terisi melalui `scripts/syncProductZatAktifDosis.ts`. Ditampilkan pada sublabel product picker (`buildProductOptions` di `LineItemEditor.tsx`, ikut ter-filter saat pencarian) ditambah pada info bar setelah produk dipilih. |
| 31 | Kriteria Produk "Produk Pernah Di PSSP" | Badge "Pernah PSSP · Pelunasan 3 Bln XX%" pada product picker, berwarna hijau ≥80% / kuning 40-79% / merah <40%, discope berdasarkan histori PSSP dokter+outlet 3 bulan terakhir. |
| 32 | Warna Low Hanging Fruit (Ada Sales = Oranye, Tidak Ada Sales = Kuning) | Menggunakan field `OutletProductKriteria.kategori`. 07-20: badge ini diubah menjadi dot warna polos (tidak lagi menampilkan teks "Low Hanging Fruit") pada product picker — badge kriteria lain (standarisasi/kompetisi) tetap menggunakan teks seperti biasa. |
| 33 | Sinkron dengan kunjungan & PSSP aktif (terlihat mana yang dikunjungi/tidak) | ⚠️ **Perlu diverifikasi** — tidak ditemukan fitur cross-reference kunjungan↔PSSP-aktif di kode berdasarkan pemeriksaan 07-20. Apabila maksudnya berbeda dari yang diperiksa, mohon dijelaskan lokasinya di app agar dapat ditindaklanjuti. |
| 34 | Detail periode untuk pelunasan yang buruk | Bagian dari panel Histori PSSP pada form MR (nama RS/outlet tiap kontrak + filter ke outlet yang sedang dipilih) — selesai 07-20. |
| 35 | Detail running rate 3 bulan terakhir | Bagian dari panel Histori PSSP pada form MR (running rate pace pelunasan untuk kontrak aktif) — selesai 07-20. Sisa dari panel ini: sumber data "Pak Eko"/item NON ESTIMASI, masih CLARIFY (lihat bagian #38 / bagian bawah). |
| 37 | Tim NSM Ex-Hospinet tidak bisa login ke sistem | Kemungkinan besar = fix "Case-insensitive login" (selesai). ⚠️ Tidak ada jejak eksplisit "Hospinet" di kode/commit — apabila masih ada laporan gagal login dari tim ini, perlu diperiksa kasus spesifiknya (kemungkinan nip/username berbeda kapitalisasi atau isu lain). |
| 44 | Diskon: gunakan DPL apabila ada, fallback ke history diskon apabila tidak ada | **Selesai & data terimpor 07-21** — model baru `DiskonHistory` (kodePI+kodeProduk → rata-rata tertimbang "% Total Diskon"), diisi dari `internal/08062026 Data Diskon All Product Jan-Apr'26.xlsx` (sheet "Raw Data", 490.611 baris, file 196MB — dibaca menggunakan ExcelJS streaming reader agar tidak OOM) melalui script baru `scripts/importDiskonHistory.ts` → **201.963 pasangan outlet+produk** teragregasi (periode sumber 202601-202604). "Item Kode" pada file ini dikonfirmasi match langsung ke `Product.kodeProduk` (diperiksa manual: "000070"→NEBACETIN, "013390"→ACETRAM, sesuai). `resolveDiskonPctWithHistory()` di `LineItemEditor.tsx` mencoba DPL (`DiskonKontrak`) terlebih dahulu, baru fallback ke history apabila DPL tidak ada — DPL tetap prioritas, history hanya fallback sesuai arahan. |
| 45 | Apabila ASM/SM vacant, approval langsung ke NSM (skip level) | **Selesai 07-21** — `resolveNextHolder()` (`poaWorkflow.ts`) diubah dari fixed-hop (`.reportsTo.reportsTo.reportsTo`) menjadi menyusuri chain berdasarkan **role**, sehingga otomatis melewati level yang vacant. Data-nya juga diperbaiki: `importStrukturVerifiedKAM.ts` sekarang skip-link `nipAtasan` saat import (sebelumnya dibiarkan `null` apabila ASM/SM vacant, sekarang lompat ke level di atasnya) — hierarki lama di-backfill ulang. |
| 46 | ASM dapat menginput POA untuk tim yang vacant | **Selesai 07-21** — namun scope-nya diperbaiki dari implementasi pertama: bukan "seluruh subordinate ASM/SM/NSM kosong", melainkan **per outlet** — apabila outlet tertentu MR dan ASM-nya vacant, SM yang dapat menginput POA khusus untuk outlet itu (apabila SM juga vacant, baru NSM), terlepas dari apakah tim lain di bawah SM tersebut lengkap atau tidak. Field baru `Outlet.coveredByNip`/`coveredByRole` (dihitung saat import, menentukan siapa yang secara efektif meng-cover outlet tersebut). `canCreatePoa()` (`authz.ts`), `getOutletsByUser()` (`masterData.ts`, membatasi scope outlet picker hanya ke outlet yang di-cover), `canView`/`canEdit`/`getVisiblePoaFilter` (digeneralisasi "pemilik POA selalu dapat mengakses", tidak lagi hardcode role MR), tombol submit pada dashboard & Detail POA (tidak lagi hardcode `isMR`) — semua disesuaikan. |
| 54 | Satuan Produk Albusmin Box→Kapsul | **Selesai 07-27** — sesuai konfirmasi user, hanya label yang diubah: field `Product.satuan` untuk kodeProduk `0304507` ("ALBUSMIN 3 BLISTER @ 10 KAPSUL") diperbarui langsung di DB dari `"BOX"` → `"KAPSUL"`. `hna` dan `konversiPembagi` (30) sengaja TIDAK direcalculate (user memilih opsi "hanya ganti label, HNA tetap") — sehingga HNA yang tersimpan masih harga per-box lama, hanya labelnya sekarang menyebut per-kapsul. 6 kode Albusmin lain di DB `satuan`-nya kosong (`"—"`), tidak disentuh. |
| 56 | Perubahan Istilah: GROWTH PSSP → GROWTH Pelunasan | **Selesai 07-27** — hanya 2 titik ditemukan di seluruh `src`: label kartu "Growth PSSP (3 Bln Terakhir)" → "Growth Pelunasan (3 Bln Terakhir)" ditambah komentar kode, keduanya di `LineItemEditor.tsx`. |
| 62 | Hapus Labeling Pelunasan | **Selesai 07-27** — `computeLabelCustomer()` di `LineItemEditor.tsx` sekarang mengembalikan `"Akan Selesai"` / `"Pernah PSSP"` polos (tanpa qualifier) apabila pelunasan <80%, bukan lagi `"...Pelunasan Buruk"`. Label `"...Pelunasan Bagus"` (≥80%) TIDAK dihapus/diubah — hanya yang "Buruk" yang dihilangkan sesuai permintaan. `LabelCustomerBadge` disederhanakan, cabang warna merah (`isBad`) dihapus karena sudah tidak ada label yang match. |
| 60 | Label Retensi Dokter | **Selesai 07-27** — badge oranye "Retensi" pada `ContractCard` (panel Histori PSSP, `LineItemEditor.tsx`), muncul apabila kontrak masih aktif DAN `prdAkhir`-nya jatuh pada salah satu bulan quarter kalender berjalan (helper baru `currentQuarterMonths()`, menggunakan `quarterToMonths()` dari `quarterUtils.ts`). Periode berakhir (`prdAwal – prdAkhir`) sendiri sebenarnya sudah ditampilkan sejak sebelumnya, hanya badge-nya yang baru. |
| 61 | Sorting Dokter PSSP | **Selesai 07-27** — dropdown pilih dokter (`customerOptions` di `LineItemEditor.tsx`) sekarang sort utama berdasarkan jarak (dalam bulan) `latestPrdAkhir` kontrak PSSP terakhir ke akhir quarter kalender berjalan (semakin dekat/mendesak, semakin atas), sort-by-pelunasan-terbaik (2026-07-24) menjadi tie-breaker kedua. Field baru `latestPrdAkhir` ditambahkan ke `PsspStatusByCustomer` (`customer.ts`). Dokter yang belum pernah PSSP tetap berada di bawah. **Pembaruan 07-27 (susulan)**: sublabel dropdown sekarang juga menampilkan periode PSSP terakhir mentah, contoh "PSSP 202601–202606 (Berjalan)" atau "(Selesai)" — field baru `latestPrdAwal` ditambahkan ke `PsspStatusByCustomer`, status Berjalan/Selesai berasal dari `isActive` yang sudah ada. **Pembaruan 07-27 (susulan #2)**: badge tag2 dropdown sekarang juga menunjukkan "· Retensi" dengan warna oranye apabila kontrak masih aktif DAN berakhir pada quarter kalender berjalan — aturan sama persis seperti badge "Retensi" pada `ContractCard` (#60), sehingga konsisten antara dropdown dan panel Histori PSSP. |
| 52 | Sidebar "Produk Rekomendasi" (rename dari "Kriteria Produk") | **Selesai 07-27** — tab hijau di-rename menjadi "Produk Rekomendasi" (2 titik teks: pill switcher + collapsed edge tab). Urutan section pada `KriteriaProdukPanel` diubah sesuai permintaan: **Pernah PSSP** (sort pelunasan terbaik, sudah ada sebelumnya) → **Produk Kontes PM** → **Produk Survey** (BARU — mengambil `getSurveyRekomendasiByOutlet` langsung pada panel ini, sesuai konfirmasi user). Section "Listing Corporate - Ada/Tidak Ada Sales" TETAP ada, ditempatkan setelah 3 section tersebut (user memilih "tetap ada", bukan "hapus"). Tab terpisah "Data Survey" (oranye) TETAP ada apa adanya (user memilih "biarkan terpisah") — sehingga data survey sekarang sengaja muncul di 2 tempat (tab sendiri + section baru ini), bukan bug. **Pembaruan 07-27 (susulan)**: panel "Produk Kontes PM Belum Diajukan" (`ProdukKontesPanel`) dipindahkan dari tab "Histori PSSP" ke tab "Produk Rekomendasi" ini (ditempatkan di atas `KriteriaProdukPanel`) — sebelumnya sempat muncul di 2 tab, sekarang hanya di sini. |
| 53 | Sorting Data Survey | **Selesai 07-27** — fungsi baru `sortSurveyRows()` (`LineItemEditor.tsx`), digunakan pada KEDUA tempat yang merender `SurveyRekomendasiRow[]` (`SurveyDataPanel` tab "Data Survey" + section "Produk Survey" pada `KriteriaProdukPanel`) agar konsisten. Urutan: Produk Kontes PM terlebih dahulu (`getAllPakets().length > 0` — definisi sama seperti Status Kontes/Non-Kontes pada #48), kemudian `potensiBulan` descending di dalam masing-masing grup. |
| 51 | Fitur Sorting pada Summary | **Selesai 07-27** — tab "outlet"/"customer" sudah lama default sort by `gapVsRealisasi` (GAP tertinggi), yang baru: kontrol UI toggle "GAP Tertinggi" / "Estimasi Tertinggi" (query param `?sort=gap\|estimasi`, pola link sama seperti filter periode yang sudah ada), GAP tetap default. Toggle hanya muncul pada tab outlet/customer (produk/mr tidak memiliki konsep GAP-vs-realisasi, selalu sort by estimasi). **Pembaruan 07-27 (susulan)**: memperbaiki urutan mode GAP — sebelumnya outlet TANPA data realisasi (realisasi=0) ikut berada di atas karena `gapVsRealisasi = estimasi - 0` terhitung sebagai "gap besar" secara matematis, padahal tidak ada perbandingan riil. Sekarang outlet yang MEMILIKI data realisasi diprioritaskan terlebih dahulu (sort by gap descending di antara mereka), baru sisanya (tidak ada data) di bawah, sort by estimasi. **Pembaruan 07-27 (susulan #2, sesuai spec terbaru dari stakeholder)**: kriteria "memiliki data" DIUBAH dari "memiliki Realisasi" menjadi "memiliki PSSP Aktif" (`estimasiAktif > 0`) khusus tab outlet — sehingga urutannya: outlet dengan PSSP Aktif terlebih dahulu (sort GAP tertinggi di antara mereka), baru sisanya (sort by estimasi). Tab "customer" TETAP menggunakan kriteria Realisasi lama (tidak memiliki konsep PSSP Aktif per-customer). |
| 65 | Lock Edit Logic | **Selesai 07-27** — sesuai konfirmasi user: (1) begitu ASM/SM/NSM meng-approve/mengedit POA yang bukan miliknya, seluruh level DI BAWAH level tersebut terkunci dari edit (levelnya sendiri dan di atasnya tetap dapat mengedit); (2) deteksi dilakukan melalui scan `PoaAuditLog` (bukan field baru pada schema) — fungsi baru `getEditLockLevel()` (`authz.ts`) berjalan mundur dari log terbaru, berhenti begitu menemukan `toStatus` DRAFT/REVISI (titik reset siklus), mengambil level tertinggi dari actor non-owner yang melakukan APPROVE/UPDATE; (3) unlock otomatis begitu POA kembali ke REVISI (melalui Reject biasa ATAU Cancel Approved NSM #64 — keduanya sudah ada, tidak perlu tombol unlock baru). `canEdit()` sekarang memeriksa lock ini terlebih dahulu (ADMIN dikecualikan). UI: banner kuning baru pada `poa/[id]/page.tsx` ("POA ini terkunci untuk diedit — sudah ada tindakan dari level X ke atas...") muncul apabila `!userCanEdit` DAN role user MR/ASM/SM/NSM (agar GM/SFE yang memang read-only sejak awal tidak diberi pesan yang keliru). ⚠️ Trade-off yang disadari: menambah 1 query `PoaAuditLog` pada setiap pemanggilan `canEdit()` (dipanggil sering — render halaman, submit, delete, dll) — user memilih opsi ini (bukan field baru) demi zero-migration, tetapi apabila terasa lambat di production, dapat dipertimbangkan untuk berpindah ke field cache pada `PoaForm`. |
| 55 | Download Excel: sheet Summary Per Outlet | **Selesai 07-27** — sheet baru "Summary Per Outlet" (menjadi sheet ke-5) pada `export/team/route.ts`, mereuse metrik yang sama seperti Matriks Summary Per Outlet (#47): Estimasi Aktif+Pengajuan, User PSSP (Aktif+Estimasi), Variasi Produk Kontes/Non-Kontes, Budget, Cost Ratio, Sales Aktif (2026), Estimasi Per User, Listing Fee — discope ke tim yang sedang di-export (`assignedOutlets`, bukan company-wide), menggunakan data yang SUDAH diambil untuk sheet lain (lineItems, activePsspAll) ditambah 2 query groupBy baru (ListingFeeKontrak, OutletSalesValueMonthly). Mengikuti konvensi file ini: DRAFT/REVISI tetap terhitung (bukan status-gated). |
| — | Download Excel: angka muncul tanpa menunggu status APPROVED | **Audit 07-27, ternyata sudah lama selesai** — baik `export/[id]/route.ts` maupun `export/team/route.ts` tidak memiliki status-gating apa pun pada angka (Estimasi/PSSP/budget dihitung dan ditampilkan langsung dari `PoaLineItem`, terlepas dari status). Komentar eksplisit pada `export/team/route.ts:84` mengonfirmasi hal ini disengaja ("this team rekap includes every status"). |
| — | Entertain Default: 1 | **Audit 07-27, ternyata sudah lama selesai** — `emptyProdukEntry()` (`LineItemEditor.tsx:118`) dan saat memilih produk (`:745`) mengatur `persenEntertain: "1"`. Komentar pada kode: "0 dibaca sebagai 'field terlupa diisi' dalam praktiknya, sehingga 1 dipilih sebagai default non-zero yang disengaja." Ini merupakan keputusan TERPISAH dari fallback diskon/listing fee yang dijadikan 0 (lihat item #17). |
| — | Approval Backdoor NSM | **Audit 07-27, ternyata sudah lama selesai** — `authz.ts:254-264` & `poaWorkflow.ts:235-260`: NSM dapat langsung meng-approve POA menjadi `APPROVED_BY_NSM` dari status apa pun (`SUBMITTED_TO_ASM/SM/NSM`), melewati pengecekan `currentHolderId`/urutan chain — selama POA tersebut masih berada di dalam subtree NSM tersebut. Komentar kode mengutip permintaan bisnis 2026-07-23 secara langsung. |
| — | Jumlah user per spesialisasi + sorting dropdown by user terbanyak | **Audit 07-27, ternyata sudah lama selesai** — `specOptions` (`LineItemEditor.tsx`) menghitung jumlah customer per label spesialisasi, memberikan sublabel "N dokter terdaftar", `.sort((a,b) => b._count - a._count)` descending. |
| — | Hide fitur "Target Produk" untuk role NSM | **Audit 07-27, ternyata sudah lama selesai** — `Sidebar.tsx`: nav item "Target Produk" hanya di-gate `roles: ["ADMIN"]`. Komentar kode: "NSM temporarily hidden from sidebar (2026-07-24) — re-add 'NSM' to bring it back." |
| — | Indikator status dokter (Pernah PSSP Baik/Buruk) + % pelunasan terakhir saat pemilihan | **Audit 07-27, ternyata sudah lama selesai** — sama seperti item #61 di atas: `psspStatusByCust` + `customerOptions` (`LineItemEditor.tsx`) membuat `tag2 = "Pernah PSSP · Pelunasan Terakhir X%"` dengan warna hijau/kuning/merah pada threshold 80%/40%, muncul pada dropdown pilih dokter SEBELUM MR memilih siapa pun (2026-07-24). |
| — | Layar Dashboard BY MR tidak dibatasi 10, dapat di-scroll | **Audit 07-27, ternyata sudah lama selesai** — `dashboard/page.tsx`: default `pageSize = 25` (dari query param `?size=`), dan `?size=all` memberikan `pageSize = null` (unlimited). Tidak ada `slice(0, 10)`/limit-10 hardcoded di mana pun pada file ini. |
| — | Kriteria Produk: "Produk standarisasi - tidak ada sales" → "Produk sudah listing corporate - tidak ada sales" | **Audit 07-27, ternyata sudah lama selesai** (2026-07-24) — `formatKriteriaLabel()` (`LineItemEditor.tsx:30-31`) me-rename seluruh tampilan "Terstandarisasi" menjadi "Listing Corporate" pada badge picker produk; data mentah `kriteriaBaru` pada DB TETAP raw "Terstandarisasi" (hanya matching-nya yang tetap harus menggunakan raw value, hanya teks yang ditampilkan yang diganti). |
| 26 | Format data survei SFE ke sistem + Sidebar Data Survey | **Dipindahkan dari ON-PROSES ke DONE 07-27, audit ternyata sudah lama selesai penuh, bukan sekadar rencana** — `scripts/importSurveyRekomendasi.ts` benar-benar mengimpor `internal/17062026 Data Rekomendasi Final.xlsx` ke model `SurveyRekomendasi` per (dokter, outlet, produk), dengan validasi kodeDokter/kodeRS ke DB. `SurveyDataPanel` (`LineItemEditor.tsx`) mengambil data ini melalui `getSurveyRekomendasiByOutlet` (`customer.ts`), merender kartu per produk rekomendasi + badge kompetitor, sudah full-function (loading/empty state dll), menempel pada tab sidebar oranye "Data Survey". ⚠️ Catatan: stakeholder meminta "split 2 tabel" (produk rekomendasi vs kompetitor) — yang ada sekarang hanya SATU list kartu, dengan kompetitor sebagai badge kecil NESTED di dalam tiap kartu, bukan tabel terpisah — apabila maksudnya harus 2 tabel visual yang benar-benar terpisah, ini masih memerlukan pekerjaan tambahan. |
| 36 | Struktur AP.K24 Matraman (Jakarta Timur) → seharusnya kategori B | **Diverifikasi 07-27, ternyata sudah benar** — query langsung ke DB production: outlet `F1011627` ("K-24 MATRAMAN, APT") field `kategori` = **"B"** persis seperti yang diminta. Tidak perlu perbaikan apa pun, hanya memang belum pernah diperiksa manual sebelumnya. |
| 64 | Cancel Approved NSM | **Selesai 07-27** — NSM dapat membatalkan approval yang sudah diberikannya sendiri, POA kembali ke status `REVISI` (konsekuensinya sama persis seperti reject: wajib direvisi ulang, version number naik) — sesuai konfirmasi user. Enum baru `AuditAction.CANCEL` (migration `20260727150000_add_audit_action_cancel`). Fungsi baru `cancelApprovedByNsm()` (`poaWorkflow.ts`, pola sama persis `rejectPoa()`) + `canCancelApproved()` (`authz.ts`, gate: role NSM DAN status genuinely `APPROVED_BY_NSM` — berbeda dari `canApprove` yang holder-gated, karena `currentHolderId` sudah `null` begitu fully approved) + `cancelApprovedByNsmAction()` (`poa.ts`, wajib mengisi alasan sama seperti reject). UI: kartu "Batalkan Approval" baru muncul pada `poa/[id]/page.tsx` apabila `isFullyApproved && userCanCancelApproved`, tepat di bawah banner "sepenuhnya disetujui" — sudah diverifikasi secara visual melalui Playwright (login sebagai NSM asli `P030966`, POA `f26d2786-...`, tombol + textarea alasan muncul dengan benar). Label Riwayat Aktivitas: "membatalkan approval (kembali ke Revisi)". |
| 47 | Matriks Summary Per Outlet: kolom baru | **Selesai 07-27** — tab "Per Outlet" pada `/summary` (`summary/page.tsx` + `TerritoryTable.tsx`) sekarang memiliki 8 kolom baru: **Estimasi Aktif+Pengajuan** (menggabungkan `estimasi` pengajuan existing + `estBaris` dari kontrak PSSP aktif via `getActivePsspByOutlets`, di-groupby `kdOutlet`), **User PSSP (Aktif+Estimasi)** (union kdCust aktif ∪ kodeCust draft), **Variasi Produk (Kontes/Non-Kontes)** (format "X/Y"), **Budget** & **Cost Ratio** (formula existing dari `poa/[id]/page.tsx`/`export/team/route.ts`: budget = Σ rencanaTotalBiaya×(psspPct+discPct+entPct), ratio = budget/estimasi — TIDAK menggunakan denominator aktif+pengajuan, tetap konsisten dengan definisi lama), **Sales Aktif (2026)** (data riil dari `OutletSalesValueMonthly`, periode ≥202601 — **9.289 baris data asli tersedia**, diverifikasi bukan nol semua), **Estimasi Per User**, **Listing Fee** (agregat baru `ListingFeeKontrak.value` groupBy `kdOutlet` — belum pernah ada agregat per-outlet sebelumnya). Diverifikasi render di browser (Playwright, login ADMIN) — seluruh kolom menampilkan angka asli, contoh baris pertama: Estimasi Rp135,4 Jt, Budget Rp20,3 Jt, Cost Ratio 15.0%. ⚠️ **Catatan performa**: untuk role ADMIN (company-wide, seluruh MR) halaman ini menjadi lambat — **58-74 detik** load time saat diuji, jauh lebih lambat dari sebelumnya karena agregat baru (active PSSP + sales + listing fee) melakukan query lintas SELURUH outlet company-wide. Untuk NSM/SM/ASM (scope lebih kecil, subtree sendiri) kemungkinan jauh lebih cepat, tetapi belum diuji langsung — perlu diperiksa apabila dirasakan lambat di production. ⚠️ **Bug ditemukan dan diperbaiki 07-27**: kolom **Listing Fee** awalnya keliru (kebesaran) — `ListingFeeKontrak.value` merupakan nilai TOTAL KONTRAK per `noreq`, tetapi DIDUPLIKASI pada tiap baris produk dalam kontrak yang sama (pola sama persis seperti `PsspKontrak.biaya`, lihat `computeActivePsspStats` di `activePssp.ts`). Query awal melakukan `groupBy`+sum seluruh baris tanpa dedupe, sehingga kontrak dengan N produk terhitung N kali (diketahui dari laporan user: 1 outlet menampilkan Rp1,3 M padahal seharusnya hanya Rp70,85 Jt — 18.5x kebesaran, persis sesuai count 32 baris = 2 kontrak unik). Perbaikan: mengganti `groupBy` menjadi `findMany` + dedupe per `(kdOutlet, noreq)` sebelum di-sum, pada KEDUA tempat yang menggunakan agregat ini (`summary/page.tsx` #47 dan `export/team/route.ts` #55). Diverifikasi ulang ke DB langsung — outlet `G3000061` sekarang benar Rp70.850.000. ⚠️ **Klarifikasi kolom Realisasi Sebelumnya (fitur lama 07-24, bukan bagian dari #47)**: user melaporkan kolom ini menampilkan "—" pada hampir seluruh baris — diperiksa ke DB, ternyata BUKAN bug, melainkan data sparse: dari 20 outlet yang sedang di-drafting (POA non-draft), hanya **3** yang memiliki histori `PsspKontrak` sama sekali. Tergantung scope role yang login, wajar apabila terlihat kosong semua. **Pembaruan 07-27 (susulan)**: kolom "Estimasi Aktif+Pengajuan" sekarang menampilkan breakdown Aktif vs Pengajuan sebagai baris kecil di BAWAH angka total, di dalam sel yang sama (bukan kolom baru/lebih lebar) — format "Aktif Rp X · Pengajuan Rp Y". **Pembaruan 07-27 (susulan #2, spec terbaru)**: kolom ke-9 baru **"Pelunasan (%) Running Rate"** ditambahkan — per kontrak PSSP aktif pada outlet tersebut, "expected lunas by now" = `estBaris × (bulan_berjalan/total_bulan_kontrak)` (formula sama persis seperti badge Running Rate per-kontrak pada `ContractCard`, `LineItemEditor.tsx`, dikonfirmasi user bahwa maksud "running rate" adalah persentase yang SEHARUSNYA sudah selesai dari periode berjalan). Diagregat ke level outlet sebagai (total lunas aktual) ÷ (total expected-lunas) — bukan rata-rata persentase per kontrak — agar 1 kontrak yang parah ketinggalan tidak "tertutupi" oleh kontrak lain yang on-time. Warna threshold sama seperti Running Rate existing: hijau ≥100%, kuning 70-99%, merah <70%. |
| 48 | Matriks Summary Per Produk (rename dari "Summary Detail Produk") | **Selesai 07-27** — tab "Per Produk" pada `/summary` sekarang memiliki kolom: **Status** ("Kontes"/"Non-Kontes", menggunakan `getAllPakets()` sesuai konfirmasi user — bukan `getProductTier()` yang spesialisasi-aware), **Estimasi Aktif+Pengajuan** (PSSP aktif di-match berdasarkan **nama produk ternormalisasi**, BUKAN kodeProduk — karena `PsspKontrak.kdProduk` berada di namespace berbeda dari `Product.kodeProduk`, "Procode ≠ Item Kode", sesuai konvensi existing pada `computeOldEstPerMonth`), **User Aktif PSSP** (distinct kdCust dari kontrak aktif yang match nama produk), **Budget**/**Cost Ratio** (formula sama seperti #47), **Sales Aktif (2026)** (DIDERIVE dari `OutletSalesMonthly.qty × Product.hna` — **bukan** angka Rupiah riil observed seperti pada outlet, karena memang tidak ada model sales value per-produk; qty asli tersedia **51.227 baris** melalui `itemKode` yang match langsung ke `Product.kodeProduk`), **Sales Per User**, **AVG Pasien Per User** & **AVG ST per Pasien** (formula `qtyProdukResep / jumlahPasienHari` sesuai konfirmasi user, dari field `PoaLineItem.jumlahPasienHari`/`qtyProdukResep` — nullable, difilter terlebih dahulu sebelum di-average). Diverifikasi render di browser — baris pertama contoh: INTRIX 1G INJEKSI, Status Kontes, Estimasi Rp4,2M, User Aktif PSSP 159, Sales Aktif Rp6,0M, Sales Per User ≈Rp37,9Jt (secara matematis konsisten: 6M/159≈37,9Jt). AVG Pasien/AVG ST menampilkan "—" untuk baris yang field `jumlahPasienHari`/`qtyProdukResep`-nya belum diisi (sesuai ekspektasi, field opsional). |

## 🟡 ON-PROSES (3)

| No | Item | Catatan |
|---|---|---|
| 1 | Struktur baru *(top urgent!!)* | **Progres besar 07-20/07-21**: mengimpor `internal/Struktur Verified Part KAM.xlsx` (sheet "ALL", 7334 outlet, NIP langsung per level GM/NSM/SM/ASM/SPV/MR) melalui `scripts/importStrukturVerifiedKAM.ts` — sekarang **365 User** di-upsert + **350 hierarki** (nipAtasan) tersambung, **3799 outlet mendapat assignment MR baru/ter-override**, ditambah `Outlet.coveredByNip`/`coveredByRole` per outlet (3799 MR normal · 808 vacant→ASM · 383 vacant→SM · 2344 vacant→NSM · **0 tidak ada siapa pun**). **07-21 perbaikan "SPV is MR"**: kolom SPV & MR pada file tersebut ternyata dua nama untuk level yang sama (hanya salah satu terisi per baris) — `resolveCoverage()` dan `MrOutletAssignment` sebelumnya hanya memeriksa kolom MR saja, sehingga ~1824 outlet yang hanya terisi kolom SPV (MR-nya kosong) keliru dianggap "MR vacant" dan ter-escalate ke ASM/SM/NSM. Diperbaiki menggunakan `effectiveMrNip(r) = r.mrNip ?? r.spvNip` pada kedua tempat tersebut — coverage "MR" naik dari 1975→3799, dan 197 outlet yang sebelumnya genuinely tidak ter-resolve sekarang seluruhnya ter-cover. Dua bug data pada kolom "NIP NSM" ditemukan dan diperbaiki melalui hardcode override pada script: **"EKA"** (07-20, berisi rangkaian kode acak per baris — NIP asli `P260205`) dan **"DODY ALWARDY"** (07-21, kosong di seluruh 580 barisnya — NIP asli `P250442`, tanpa perbaikan ini 197 outlet di bawahnya tidak memiliki fallback sama sekali). Sisa: 2685 outlet masih belum memiliki MR yang ter-resolve (assignment lama tetap digunakan) + 850 outlet dengan NIP MR terisi tetapi nama placeholder (sisa data lama) — ini genuinely merupakan keputusan bisnis manual, bukan lagi masalah data. Kasus spesifik "AP.K24 Matraman Jakarta Timur → seharusnya kategori B" **sudah diverifikasi 07-27** — diperiksa langsung ke DB production, outlet `F1011627` kategorinya memang sudah "B" (lihat #36 pada tabel DONE). ⚠️ Stakeholder menambahkan tanda ✅ di sebelah item ini tetapi labelnya masih "(ON-PROSES)" — item ini tetap ditempatkan di sini karena progresnya memang belum 100%, tetapi apabila maksudnya sudah dianggap cukup, tinggal disampaikan saja. **Klarifikasi 07-27**: stakeholder juga menyebutkan "SPV dirubah jabatannya menjadi SPV dari MR" sebagai item terpisah — audit kode menemukan ini MEKANISMENYA SAMA dengan perbaikan "SPV is MR" di atas (`effectiveMrNip`), ditambah field `User.jabatan` (schema) yang memang sudah ada untuk meng-override display-only title "SPV" walaupun role sebenarnya MR. Bukan role/enum baru — tidak ada `SPV` pada `enum Role` (`schema.prisma`). **Pembaruan 2026-08-06**: spec SDD draft dibuat di `docs/outlet-nexus-migration/` untuk migrasi sumber `outletSync.ts` dari MSSQL ke Nexus API (`get_outlet_by_nip`) — cutover sumber outlet + assignment, tapi field `kategori`/`coveredByNip`/`coveredByRole` di item ini TETAP dari `importStrukturVerifiedKAM.ts`, tidak ikut pindah (belum ada endpoint Nexus untuk itu). Belum diimplementasikan, ada 1 open question blocking (outlet dengan rantai kepemilikan vacant total tidak ter-cover pendekatan per-NIP) — lihat `docs/outlet-nexus-migration/01-business-rules.md` §6 OQ-1. |
| 22 | Notes alasan reject dari atasan | Tombol Reject eksplisit sudah ada, wajib mengisi alasan, tercatat pada Riwayat Aktivitas — apabila masih dianggap on-proses, kemungkinan terdapat scope tambahan yang diminta (misalnya notifikasi ke MR, atau tempat tampil lain) yang perlu diperjelas. |
| 25 | History Visit sebelumnya (warna abu-abu, tidak perlu tabel) | Placeholder "Hari Praktek/Bln" sudah selesai. Sisa: histori jumlah visit (angka, bukan hanya teks status) ditempatkan di sebelah field "Rencana Visit/Bulan", styling teks abu-abu saja sesuai arahan. ⚠️ Audit 07-27: stakeholder mengklaim ini DONE ("Masukan History Visit sebelumnya Proses Input") tetapi tidak ditemukan jejaknya di kode sama sekali (tidak ada grep match untuk "bulan lalu"/"riwayat visit"/angka historis di sekitar field "Rencana Visit/Bulan") — tetap ditempatkan ON-PROSES sampai ada klarifikasi/tautan ke lokasi di app. **Pembaruan 2026-08-04**: sumber data untuk ini SEKARANG sudah ada (lihat #73 pada tabel DONE — API eksternal Exodus Activity), tetapi belum dipasang pada lokasi spesifik yang diminta di sini (di sebelah "Rencana Visit/Bulan"); #73 dipasang pada sidebar "Histori PSSP" (per outlet+customer), bukan pada titik ini. Apabila maksudnya memang pada titik ini, tinggal mereuse `getVisitHistoryByCustomerOutlet`. |

## ❓ NEED CONFIRMATION (8)

| No | Item | Pertanyaan/status |
|---|---|---|
| 10 | PSSP/KPDM per outlet | *"seharusnya kpdm dan dpl itu per outlet, dan sudah teridentifikasi dari pengisian awal"* — belum ada keputusan/implementasi. ⚠️ Audit 07-27: stakeholder mengklaim "Pilihan Dropdown USER, KPDM" DONE — grep "KPDM" pada seluruh `src/` hanya ditemukan pada komentar `persenPsspKpdm` (field budget %, tidak ada hubungannya dengan dropdown per-outlet ini). Tidak ada dropdown/field KPDM apa pun pada kode. Klaim DONE-nya tidak cocok dengan kode, tetap NEED CONFIRMATION. |
| 11 | Pilihan PS / SP / peremajaan | Kemungkinan terkait dropdown "Jenis PSSP" yang sudah dikerjakan (full-stack selesai tetapi di-hide sementara dari UI). Belum jelas apakah cukup dropdown sederhana atau memerlukan level otomasi. ⚠️ Audit 07-27: stakeholder JUGA mengklaim "Pilihan Dropdown PS/SP" DONE pada daftar terpisah — tetapi kode `LineItemEditor.tsx` masih `{false && (...)}` yang membungkus dropdown Jenis PSSP ini (comment: "hidden for now per request... field + logic kept intact for a quick re-enable later"). Klaim DONE-nya bertentangan dengan kode DAN bertentangan dengan daftar NEED-CONFIRMATION stakeholder sendiri (item ini masih ada di situ juga) — kemungkinan salah centang di sisi mereka. Tetap NEED CONFIRMATION. |
| 12 | Konsep tabungan | Desain belum dipikirkan. |
| 13 | Ratio target 140% dihilangkan | Masih ada pada kode (`DraftChecklist.tsx`: `ratioEst >= 140`). Perlu dipikirkan rekomendasi % pengganti / formulanya. |
| 14 | Monitoring POA lama & sebelumnya | API yang relevan bernama **"Nexus"** (api-nexus.pharos.id) — bukan "Exodus". Sudah dieksplorasi, lihat catatan pada #1. **Pembaruan 2026-08-06**: 4 endpoint Nexus tercatat lengkap — `get_employees`, `get_outlet_by_nip`, `get_customer_by_outlet` (sudah live di produksi, `customer.ts:530-611`), `get_subordinates`. Rencana migrasi outlet ke `get_outlet_by_nip` didokumentasikan di `docs/outlet-nexus-migration/`. |
| 16 | Listing fee otomatis by outlet | Data `ListingFeeKontrak` sudah diimpor, tetapi belum jelas apakah "pembagian otomatis per outlet" ini sudah terpakai pada kalkulasi POA atau masih manual — perlu konfirmasi formula. |
| 27 | Sisa budget ditampilkan value tahunan | Belum dikerjakan — perlu konfirmasi formula (budget historis?). |
| 38 | Historis Kunjungan By MR by Customer (akumulasi 3 bulan terakhir) | Item baru dari stakeholder, konfirmasi ke Pak Fakhri (melalui Anthony SFE Pharos). Belum ada implementasi/desain. ⚠️ Item ini juga sudah di-drop dari NEED-CONFIRMATION-7 versi terbaru stakeholder (tidak disebut sama sekali, bukan pada DONE maupun ON-PROSES) — statusnya menjadi tidak jelas, tetap DIPERTAHANKAN di sini sampai ada klarifikasi eksplisit. **Pembaruan 2026-08-04**: INI SEKARANG SELESAI — lihat #73 pada tabel DONE, stakeholder memberikan akses API eksternal Exodus Activity yang persis menyediakan data ini. Catatan penting juga: ini merupakan item yang sama yang membuat `docs/kpi-monitoring/01-business-rules.md` §7 menyebutkan "Call Activity TIDAK ADA data sama sekali". API ini (khususnya endpoint "Get Count Visit By NIP", BELUM digunakan, hanya "By Customer+Outlet" yang sudah) kemungkinan dapat mengisi celah tersebut juga (`plan_visit_by_period`/`actual_visit_by_period`/`hari_kerja_by_period` per NIP per bulan — persis bentuk yang dibutuhkan KPI Monitoring pilar Call Activity). Belum diintegrasikan ke KPI Monitoring, hanya dicatat di sini agar tidak terlewat. |

## ⬜ BELUM DIKERJAKAN — item baru 07-21 (#39-43)

*(Tidak ada tag status dari stakeholder pada 5 item ini, sehingga ditempatkan di sini apa adanya. Beberapa sudah di-cross-check secara cepat ke kode, disertai catatan; sisanya belum sempat digali.)*

| No | Item | Catatan |
|---|---|---|
| 39 | Check COUNT OF PRODUCT di master Produk, check HNA (harus mengikuti kenaikan harga per Juli) | Audit data — belum diperiksa. Perlu diketahui sumber "harga baru per Juli" berasal dari mana untuk dibandingkan dengan `Product.hna` yang ada sekarang. ⚠️ Terkait: audit 07-27 menemukan `Product.nilaiRPersen` merupakan snapshot manual dari `scripts/syncNilaiR.ts` (import sekali dari file "Nilai R.xlsx"), BUKAN dihitung ulang otomatis dari `hna` yang berjalan — sehingga apabila HNA naik tetapi `syncNilaiR.ts` tidak dijalankan ulang, persentasenya menjadi stale. Relevan juga untuk item ini. |
| 40 | Check Struktur Hospinet / KAM | Import `Struktur Verified Part KAM.xlsx` (lihat #1) hanya mencakup divisi **KAM** — Hospinet masih belum tercakup pada struktur organisasi (org hierarchy/outlet coverage tetap tidak memiliki data Hospinet). **Progres parsial 07-21**: `internal/customer_pssp_hospinet.xlsx` diimpor melalui `scripts/importPsspHospinet.ts` — ini memberikan customer roster + snapshot PSSP level-customer untuk Hospinet (lihat #42), tetapi BUKAN struktur org/outlet-ownership Hospinet, sehingga #40 masih belum selesai sepenuhnya. |
| 41 | Fitur Log-out di Mobile | ✅ **Selesai 07-21** — koreksi catatan sebelumnya: tombol "Sign out" ternyata SUDAH ADA pada `Sidebar.tsx` (footer sidebar, form POST ke `/api/auth/logout`), bukan "belum ada sama sekali". Bug aslinya: pada mobile, drawer sidebar menggunakan `h-screen` (`100vh`) sekaligus `position: fixed` + `inset-y-0` — apabila `top`/`bottom`/`height` ketiganya terisi pada elemen fixed, `height` menang atas `bottom` (CSS2.1 §10.6.4), sehingga begitu address bar browser mobile muncul (visible viewport < 100vh), bagian bawah drawer (termasuk tombol Sign out) terdorong ke bagian layar yang tidak terlihat/tidak dapat diklik. Perbaikan: `h-screen` → `h-dvh` (dynamic viewport height, mengikuti viewport asli yang terlihat). |
| 42 | Check Database Customer terbaru | **Progres 07-21**: mengimpor `internal/customer_pssp_hospinet.xlsx` (2376 baris) melalui `scripts/importPsspHospinet.ts` — data ini bersifat flat (nama+spesialisasi+outlet+status+value PSSP/pelunasan/RR agregat, TANPA nomor kontrak/produk/breakdown bulanan), sehingga sengaja dibuatkan model baru `PsspHospinetSnapshot` (mirip `DiskonHistory` vs `DiskonKontrak`, tidak dipaksakan masuk ke `PsspKontrak`). Hasil: **1887 Customer baru** + **1919 CustomerOutlet** + **1919 PsspHospinetSnapshot** (32 nama match ke Customer existing, 357 baris di-skip karena spesialisasi kosong, 64 di-skip karena outlet tidak ada pada tabel `Outlet`). Ditampilkan pada sidebar "Histori PSSP" sebagai kartu fallback saat tidak ada histori `PsspKontrak`. ⚠️ Kosakata spesialisasi Hospinet ("Dokter Umum" dst) berbeda dari 16 kategori kurasi PM (`SPESIALISASI_PM_LABEL`) — hanya ~14% (295 baris: Anak/Kandungan/Penyakit Dalam/Saraf/Paru/Psikiatri/Ortopedi/Bedah Umum/Anestesi) yang ternormalisasi ke kategori kurasi, sisanya (termasuk "Dokter Umum" 79%) tetap raw value karena tidak ada bucket GP pada daftar kurasi — customer-customer ini tidak muncul pada dropdown spesialisasi form Tambah POA. Audit Customer database yang lebih luas (bukan hanya Hospinet) masih belum dikerjakan. |
| 43 | Optimalisasi Versi Mobile | Belum dikerjakan — scope-nya luas (responsive layout dll), belum di-breakdown menjadi task konkret. ⚠️ Stakeholder mengklaim ini DONE ("Optimalisasi Versi Mobile ✅") pada daftar DONE mereka — tetapi tidak ada scope/task konkret yang dapat di-cross-check ke kode (item ini sejak awal memang belum di-breakdown). Apabila terdapat pekerjaan spesifik yang sudah dianggap "selesai", mohon disebutkan agar dapat diverifikasi. |

## ⬜ BELUM DIKERJAKAN — item baru 07-27 (#47-66)

*(16 item baru dari stakeholder tanggal 2026-07-27 (#47-62), ditambah 3 lagi susulan pada hari yang sama (#63-65), ditambah 1 lagi susulan ketiga (#66 Layout Approval Drafting). #52/#54/#56/#60/#61/#62 (low-hanging fruit) dan #47/#48/#64 (3 fitur besar) sudah dikerjakan 07-27, dipindahkan ke tabel DONE di atas. Sisanya belum diperiksa ke kode, ditempatkan apa adanya sesuai permintaan.)*

| No | Item | Catatan |
|---|---|---|
| 49 | Input Data Target: top-down | Sifat pengisian top-down terlebih dahulu. |
| 50 | Halaman Approval By Personil: kolom wajib | Wajib menampilkan Estimasi (Aktif + Baru), Budget, dan Cost Ratio. Kemungkinan overlap/subset dari #63 (Matriks Summary Per Personil) yang lebih lengkap — perlu diperjelas apakah ini 2 halaman berbeda atau 1 fitur yang sama. |
| 57 | Summary Bawah Produk (Form POA): Growth PSSP + Growth Pelunasan | Menambahkan info Growth PSSP dan Growth Pelunasan pada bagian summary bawah tabel produk saat pengisian Form POA. |
| 58 | Growth Estimasi per Produk | Menampilkan persentase growth Estimasi POA vs Pelunasan Sebelumnya apabila POA sudah terisi. |
| 59 | Warning Logic Growth Estimasi | Growth ≤ 0: warning "Kenapa malah lebih kecil dan belum intensifikasi". Growth > 0: warning/apresiasi "Selamat sudah intensifikasi, pastikan estimasinya tepat". Terkait #58. |
| 63 | Matriks Summary Per Personil | **Item baru 07-27** — Jumlah Outlet, Jumlah User PSSP Aktif, Variasi Produk (detail Kontes & Non-Kontes: Target, Estimasi, Ach%, Jumlah User), Sales Aktif, Target, Ach%, Budget, Cost Ratio. Kemungkinan overlap dengan #50 (Halaman Approval By Personil) — lihat catatan pada item tersebut. |
| 65 | Lock Edit Logic | **Item baru 07-27** — mengunci akses edit POA begitu atasan sudah Approve atau Edit. Perlu diperjelas: siapa yang terkunci (hanya pemilik POA/MR, atau seluruh level di bawah atasan yang meng-approve tersebut?), dan apakah ada mekanisme unlock/override selain reject. |
| 66 | Layout Approval Drafting | **Item baru 07-27 (susulan kedua)** — penyesuaian layout halaman drafting/approval, dengan penekanan pada: Pengali Nilai R, Estimasi, Nilai PSSP, Pengajuan Sebelumnya (hitung Tercacah), Growth Estimasi & Pelunasan dari PSSP Sebelumnya, Jumlah Variasi & Produk Kontes, Nama Dokter (serta Label) & Rumah Sakit, Detail Target By Bulan, CR % Final. Kemungkinan besar ini terkait `poa/[id]/page.tsx`/`DraftChecklist.tsx` (Daftar User) — banyak field yang disebutkan sudah memiliki datanya (lihat #5, #18, #58/#59 growth; #7 target produk kontes), scope-nya lebih ke REORGANISASI TAMPILAN drafting existing daripada fitur data baru. Belum diperiksa detail ke kode, dan belum jelas apakah ini halaman terpisah dari Daftar User yang sudah ada atau revisi tampilannya. |

## ⬜ BELUM DIKERJAKAN — item baru 07-30 (#67)

| No | Item | Catatan |
|---|---|---|
| 67 | KPI Monitoring FF Hospital (Kinerja, Kontrak & Kaderisasi) — "Monitoring KPI Perpanjangan" | **Item baru 2026-07-30**, dari Memo Internal `SM/ETH-II/PI/08.2026` (efektif 01 Agustus 2026, menggantikan memo lama `SM/ETH-I/PI/05.2026`). Scorecard bulanan 4 pilar berbobot (Sales Achievement 50%/Call Activity 25%/Customer Expansion 15%/Kepatuhan Absensi 10%) → Total Score 0-100 → rekomendasi perpanjangan kontrak (>85=12bln, 71-85=9bln, 55-70=6bln evaluasi, <55=0bln). Spesifikasi ditulis terlebih dahulu (SDD) pada `docs/kpi-monitoring/` sebelum coding (README + business-rules, data-model, ui-and-access). **v1 diimplementasikan 2026-07-30**: halaman baru `/kpi-perpanjangan` ("Monitoring KPI Perpanjangan" pada sidebar), **ADMIN-only** (keputusan user, pola yang sama seperti `/monitoring`). Model baru `KpiMonthlyEntry`/`KpiContractEvaluation` (migration `20260730115119_add_kpi_monitoring_models`), scoring engine `src/lib/kpiScoring.ts` (diverifikasi cocok persis dengan contoh perhitungan pada memo: 73,75 → 9 bulan), server actions `src/app/actions/kpi.ts`, tabel `src/components/kpi/KpiTable.tsx`. Sales Achievement & Customer Expansion dihitung otomatis dari data yang sudah ada (sumber sama seperti Monitoring page + `getActivePsspByOutlets`). **Call Activity dan Kepatuhan Absensi TIDAK ADA data sama sekali** di sistem manapun — bukan celah baru, overlap langsung dengan **#38** (Historis Kunjungan By MR by Customer, masih NEED CONFIRMATION, belum ada jawaban Pak Fakhri) **dan #25** (History Visit sebelumnya, ON-PROSES) — untuk v1 kedua indikator ini **diinput manual oleh ADMIN per personil per bulan** (field `callActivitySource`/`absensiSource` disiapkan agar mudah diganti dengan sync eksternal nanti). ⚠️ Beberapa formula masih **ASUMSI kerja** (konfirmasi user: "lanjut, disesuaikan belakangan"), BUKAN klarifikasi asli stakeholder — lihat `docs/kpi-monitoring/01-business-rules.md` §7: (1) standar kunjungan "4+6/2+3/3" diasumsikan MR/SPV=10, ASM=5, SM=3 kunjungan/bulan; (2) unit Absensi diasumsikan rata-rata jam telat lapor/bulan; (3) Customer Expansion menggunakan proxy PSSP aktif apa adanya; (4) "PS/SP" tidak difilter ke `enum PsSp` (makna bisnisnya sendiri juga belum jelas pada schema). **Belum dibangun**: halaman/form Evaluasi Kontrak (`KpiContractEvaluation` — agregasi per periode kontrak, keputusan atasan + rasionalisasi + rencana pengembangan personil) — v1 baru mencakup scorecard bulanan, belum alur evaluasi kontrak penuh. Role matrix akhir (SM menilai ASM/SPV/MR, NSM menilai SM) juga belum di-enforce — masih ADMIN-only. **Dikonfirmasi 2026-07-30 (user)**: dashboard self-view untuk MR (dapat melihat KPI perpanjangan diri sendiri) memang merupakan fitur yang diinginkan ke depannya — TETAPI sengaja belum dibuka/dibangun sekarang, ditahan sampai halaman ADMIN-only ini matang. Lihat `docs/kpi-monitoring/03-ui-and-access.md` §4. |

## ✅ item baru 08-03 (#68-72) — dikerjakan 08-03 (sesi yang sama)

*(5 item dari stakeholder tanggal 2026-08-03, target selesai hari yang sama. Nomor stakeholder asli (#8/#10/#11/#12/#13, numbering mereka sendiri 1-80) dicatat pada tiap item — numbering internal dokumen ini tetap melanjutkan dari #67. Belum diuji manual di browser — typecheck (`tsc --noEmit`) dan `eslint` bersih di seluruh file yang diubah.)*

| No | Item | Catatan |
|---|---|---|
| 68 | Biaya Estimasi Tercacah di Ringkasan Draft (stakeholder #8) | **Ternyata sudah ada** dari pekerjaan sesi sebelumnya pada hari yang sama (belum ter-commit) — section "Tercacah (Kuartal Ini)" vs "Bukan Tercacah (Full Periode)" pada `DraftChecklist.tsx` (`StatsPanel`) sudah menampilkan breakdown PSSP Berjalan (Estimasi Aktif + Biaya Aktif via `aktifPssp.estBarisTercacah`/`nilaiTercacah`) vs POA vs Total. Tidak ada perubahan kode tambahan, tinggal di-commit. |
| 69 | Fitur Edit Quarter di Form (stakeholder #10) | **Selesai** — `updatePoaPeriodAction` (`src/app/actions/poa.ts`) + `EditQuarterControl` (komponen client baru) pada header `poa/[id]/page.tsx`, muncul apabila `userCanEdit && (isDraft \|\| isRevisi)`. Keputusan (dikonfirmasi user): mengedit `PoaForm.period` langsung, `PoaLineItem.periodeAwal` yang sudah terisi TIDAK ikut berubah — re-alignment manual menjadi tanggung jawab MR. Terdapat pengecekan duplikat (tidak boleh pindah ke quarter yang sudah memiliki draft lain milik owner yang sama). |
| 70 | Penyesuaian Label "Target Area" Menjadi "Target" (stakeholder #11) | **Selesai** — label pada `DraftChecklist.tsx` diganti menjadi "Target"/"Target ★". Kalkulasi (`poa/[id]/page.tsx`) diperluas dari MR-only (`poa.ownerId` saja) menjadi `getSubordinateMRNips(poa.owner)` — SUM `TargetHospitalValue` seluruh subordinate MR di bawah owner POA (dikonfirmasi user). POA milik MR sendiri: tetap hanya target dirinya sendiri (1 personil, tidak berubah). POA milik ASM/SM (kasus vacant-team): sebelumnya selalu "-", sekarang di-SUM dari subtree MR-nya. |
| 71 | Fix Logic Drafting: Sembunyikan "Pengajuan Sebelumnya (Tercacah - Kuartal Ini)" pada Dokter Baru / Tanpa Riwayat PSSP (stakeholder #12) | **Selesai** — tile "Tercacah (Kuartal Ini)" per dokter pada `DraftChecklist.tsx` (`DoctorRow`) sekarang di-gate `!isDokterBaru && (...)`, menggunakan flag `isDokterBaru = !first.kodeCust` yang sudah ada (digunakan juga untuk badge "Baru"). |
| 72 | Summary Spesialis Tambahan: Jumlah Customer Sebelumnya & Growth By Jumlah Spesialis (stakeholder #13) | **Selesai**, sekaligus menemukan+memperbaiki bug lama — growth **by value** pada tab "Per Spesialisasi" ternyata selalu `null` karena `realisasiSebelumnyaFor()` (`summary/page.tsx`) tidak memiliki cabang untuk tab spesialisasi (PsspKontrak tidak memiliki kolom spesialisasi langsung, perlu join ke `Customer.spesialisasi` berdasarkan `kdCust` — sekarang ditambahkan). Kolom baru "Growth Jumlah Customer" (`TerritoryTable.tsx`, di-gate `variant === "spesialisasi"`) membandingkan jumlah customer unik kuartal ini vs kuartal sebelumnya per spesialisasi. |

## ✅ item baru 08-04 (#73) — dikerjakan 08-04 (sesi yang sama)

| No | Item | Catatan |
|---|---|---|
| 73 | Histori Visit Per Outlet Per Customer (Akumulasi 3 Bulan Terakhir) (stakeholder #7) | **Selesai** — user memberikan akses API eksternal baru, **Exodus Activity** (berbeda dari "Nexus" yang disebutkan pada #14 — jangan sampai tertukar), lengkap dengan client secret develop/staging/production. Client baru `src/lib/exodusApi.ts` (auth client_credentials + token cache in-memory ~1 jam, endpoint `visits/count-by-customer-outlet`, degrade ke `null` apabila env var belum di-setup atau API-nya down — tidak pernah throw/crash halaman). Server action `getVisitHistoryByCustomerOutlet` (`customer.ts`) mengakumulasi 3 bulan terakhir. UI: panel baru "Histori Visit (3 Bulan Terakhir)" pada sidebar "Histori PSSP" (`LineItemEditor.tsx`), di bawah Histori PSSP/di atas Histori Listing Fee. Env var baru (`EXODUS_AUTH_URL`/`EXODUS_AUTH_CLIENT_ID`/`EXODUS_AUTH_CLIENT_SECRET`/`EXODUS_API_BASE_URL`) ditambahkan ke `src/lib/env.ts` (optional, tidak membuat app crash apabila kosong) + `.env.example`; **secret develop sudah diisi ke `.env.local` (gitignored) untuk pengujian lokal — secret staging/production BELUM di-set ke Vault (`env/data/century/form-poa`), perlu tindakan ops terpisah sebelum fitur ini benar-benar berjalan di staging/production.** Cross-ref: ini menutup #38 (NEED CONFIRMATION lama) — lihat catatan pada item tersebut mengenai endpoint "By NIP" yang belum digunakan tetapi kemungkinan dapat mengisi celah data KPI Monitoring pilar Call Activity juga. |

## ⚠️ Diskrepansi klaim stakeholder vs kode (audit 07-27)

*(Hasil audit kode 07-27 untuk cross-check tracker lengkap stakeholder — 20 klaim "DONE"/status ditelusuri; 8 CONFIRMED dan sudah dipindahkan ke tabel DONE di atas dengan tag "Audit 07-27". Sisanya, 2 PARTIAL dan beberapa tidak ditemukan jejak kodenya, dicatat di sini agar tidak hilang tetapi juga tidak keliru ditandai selesai.)*

**PARTIAL (ada implementasinya, tetapi tidak persis sesuai klaim):**
- **"Data Pelunasan PSSP menggunakan perhitungan VB"** — "VB" ternyata hanya nama file sumber (`internal/PSSP VB Horizontal dan Vertikal.xlsx`, sheet "PSSPVBHor"/"PSSPVBVer"), bukan istilah metode perhitungan. `scripts/importPsspKontrak.ts` hanya mengimpor sheet "Hor" (sheet "Ver" sengaja tidak digunakan, redundant). Perhitungan pelunasan % sendiri (pada `computePelunasanPct`/`export/[id]/route.ts` dan `LineItemEditor.tsx`) tetap menggunakan logic yang sama seperti sebelumnya (estBaris/totalLunas/cUrut) — tidak ada logic baru yang "berbasis VB".
- **"% NILAI R: Perhitungan dipastikan sudah mengikuti kenaikan harga terbaru"** — `nilaiRPersen` merupakan snapshot manual dari import (`scripts/syncNilaiR.ts`, sekali jalan dari "Nilai R.xlsx"), BUKAN dihitung ulang otomatis dari `Product.hna` yang berjalan. Apabila HNA naik tetapi script ini tidak dijalankan ulang, persentasenya menjadi stale. Lihat juga #39 di atas.
- **"Peletakan data survey (split 2 tabelnya dengan produk kompetitor)"** — yang ada sekarang SATU list kartu per produk rekomendasi, dengan produk kompetitor sebagai badge kecil NESTED di dalam tiap kartu (`SurveyDataPanel`/`KriteriaProdukPanel` pada `LineItemEditor.tsx`) — bukan 2 tabel visual yang benar-benar terpisah. Apabila maksudnya harus 2 tabel yang benar-benar terpisah, ini masih memerlukan pekerjaan tambahan.

**NOT FOUND (diklaim DONE, tetapi tidak ditemukan jejak kode/data sama sekali):**
- **"Check data pricil untuk dimasukkan ke spesialis"** — grep "pricil" pada seluruh repo nihil. Kemungkinan ini merupakan task manual di luar kode (data entry langsung pada admin panel/DB), tidak ada artifact yang dapat diverifikasi.
- **"Check Produk Kontes PM (apakah datanya sudah benar sesuai dengan spesialis)"** — sama, tidak ada script/audit artifact yang cocok pada `scripts/`. Kemungkinan task manual/audit spreadsheet di luar repo.
- **"Dropdown USER, KPDM"**, **"Masukan History Visit sebelumnya"** — lihat masing-masing pada tabel NEED CONFIRMATION #10 dan ON-PROSES #25 di atas, keduanya diklaim DONE oleh stakeholder tetapi audit kode tidak menemukan buktinya sama sekali. (Catatan: "Struktur AP.K24 Matraman → kategori B" awalnya juga masuk di sini, tetapi setelah diperiksa langsung ke DB production ternyata BENAR sudah "B" — dipindahkan ke DONE #36, bukan diskrepansi.)

---

## 🔧 Item tambahan (tracked internal, di luar 46-list stakeholder di atas)

*(Ini merupakan item yang sebelumnya hanya di-track secara internal — tidak ada nomor asli dari daftar stakeholder — tetapi tetap relevan untuk histori pekerjaan.)*

### ✅ Selesai

| Item | Catatan |
|---|---|
| Sort dropdown produk (produk pernah-PSSP terlebih dahulu) + qty & star breakdown pada "Total Semua Produk" | Selesai. |
| Riwayat Aktivitas: audit log untuk edit biasa (bukan hanya transisi status) | Selesai, di-extend lagi: menyimpan customer+produk yang diedit pada snapshot, ditampilkan melalui toggle "Lihat Detail" per entry. |
| Jenis PSSP dropdown per produk (PSSP/Retensi/Peremajaan/Perpanjangan) | Full-stack selesai, di-hide sementara dari UI (`{false && ...}` — logic backend tetap ada, mudah untuk di-reenable). Kemungkinan terkait item #11 di atas. |
| Bug: teks error mentah "NEXT_REDIRECT" muncul sekilas saat klik Simpan | **FIXED** — re-throw `isRedirectError()` sebelum tertangkap sebagai error biasa. |
| Bug: Pengali Nilai R = 0 tidak dikali 0 | **FIXED** — akar masalahnya "0 bersifat falsy di JS". |
| Case-insensitive login | Selesai — kemungkinan ini yang menjawab item #37. |
| Kalkulator estimasi quantity per produk | Selesai. |
| Tombol Delete draft POA pada dashboard | Hanya muncul untuk POA berstatus DRAFT. |
| Tombol "+ Daftar User Baru" menjadi toast "belum ready" | Tidak lagi bernavigasi. |
| Kartu Estimasi Sales & Nilai PSSP side-by-side | Selesai. |
| Doctor/user combobox: label spesialisasi + nama dipanjangkan | Selesai. |
| Rename "% PSSP Dokter" → "% PSSP User" | Termasuk header kolom Excel export tim. |
| PSSP Aktif ikut ke Excel export | Sheet baru "PSSP Aktif" pada export per-POA dan export tim ASM/SM/NSM. |
| Verifikasi: ASM/SM/NSM hanya dapat mengedit, tidak dapat membuat draft baru | Sudah benar sejak awal, diverifikasi di browser menggunakan 2 akun ASM real. |
| Placeholder deskriptif (Hari Praktek/Bln, Resep/Hari → Pasien Baru/Hari, Jml Produk ST/Resep) | Selesai. |
| Input Lama Periode dibuat lebih lebar | `LineItemEditor.tsx:434` — lebar 220px, sekarang lebih lebar dari field "Periode Awal" di sampingnya (200px). |

### ⬜ Belum

| Item | Catatan |
|---|---|
| Input Lama Periode dibuat nullable | ⚠️ Berdasarkan cross-check kode 07-20: **belum** — kolom `lamaPeriode` pada schema masih `Int` (`NOT NULL`, lihat migration `20260710020148_add_line_items_outlets`), dan UI pada `LineItemEditor.tsx:435` masih menggunakan `<Req/>` + validasi wajib isi pada submit handler (3 panel: Add/AddProduct/EditDoctor). Apabila memang ingin dibuat nullable, ini memerlukan migration schema + penghapusan validasi required — sampaikan saja apabila ingin dikerjakan. |
| Rawat inap vs rawat jalan | Label sudah diganti "Pasien Baru/Hari", logic pembeda formula rawat-inap-vs-jalan belum diimplementasikan terpisah. |
| ConfirmDialog custom menggantikan native `confirm()` | Sudah dipasang pada tombol "✎ Edit". 2 `confirm()` lain (hapus baris produk, hapus dokter) belum diganti. |
