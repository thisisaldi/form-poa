# REVISI LAYAR POA SYSTEM — TRACKER

*(Diupdate 2026-07-27 — direkonsiliasi penuh sama tracker resmi stakeholder (numbering mereka sendiri 1-80: DONE=54/ON-PROSES=19/NEED CONFIRMATION=7). Nomor di tabel-tabel bawah ini TETAP pakai numbering internal lama (1-65), bukan numbering 1-80 stakeholder — soalnya susunan/pengelompokannya beda. Audit kode (07-27) nemuin 8 klaim "DONE" stakeholder yang sebelumnya belum tercatat di sini tapi ternyata BENERAN ada di kode (pindah ke DONE di bawah), dan 6 klaim "DONE"/status lain yang TERNYATA gak match kode sama sekali — lihat section "⚠️ Diskrepansi" di paling bawah buat detail lengkapnya. #64 (Cancel Approved NSM), #47 (Matriks Summary Per Outlet), #48 (Matriks Summary Per Produk) dikerjain penuh 07-27 — lihat catatan masing-masing di tabel DONE, termasuk perf note utk #47. Sebut nomornya aja (numbering internal) buat mulai kerjain satu item.)*

**Ringkasan**: ✅ DONE = 47 · 🟡 ON-PROSES = 3 · ❓ NEED CONFIRMATION = 8 · ⬜ BELUM DIKERJAKAN (#39-43) = 4 · ⬜ BELUM DIKERJAKAN (item baru 07-27, #49-65) = 10 *(#41 pindah ke DONE 07-21. #44/#45/#46 pindah ke DONE 07-21. #52/#54/#56/#60/#61/#62 pindah ke DONE 07-27 (low-hanging fruit). #26 (Data Survey/SFE) pindah dari ON-PROSES ke DONE 07-27 — audit kode confirm fitur sidebar-nya udah jalan penuh, bukan cuma rencana. 7 item baru DONE ditambah 07-27 dari audit kode (lihat tabel DONE, ditandai "Audit 07-27"). #36 (AP.K24 Matraman) pindah ke DONE 07-27 — dicek langsung ke DB production, `kategori`-nya emang udah "B". #47/#48/#64 dikerjain penuh 07-27 (3 fitur besar: 2 matriks summary + Cancel Approved NSM), lihat catatan masing-masing. NEED CONFIRMATION jadi 8 (bukan 7 kayak versi terbaru stakeholder) karena #38 (Historis Kunjungan) di-drop dari list mereka tanpa penjelasan (bukan DONE, bukan ON-PROSES) — saya pertahankan sampai ada klarifikasi. #19 tetap DONE walau list stakeholder sempat nulis ON-PROSES, udah diverifikasi langsung di kode. #22 dibiarkan ON-PROSES konsisten sama list stakeholder walau kodenya sebenarnya udah ada, karena ada ambiguitas scope yang belum diklarifikasi.)*

## ✅ Beres 07-21 begitu DB remote bisa diakses lagi

- ~~Jalankan `prisma migrate deploy`~~ — ternyata udah ke-apply duluan lewat deploy pipeline (termasuk `20260720163533_grant_admin_p260054`), tapi UPDATE-nya jadi no-op karena User P260054 emang belum pernah ada.
- **User P260054 dibuat baru** (nama: M NAUFALDI FADHLIRRAHMAN, role ADMIN, isActive true) — sebelumnya gak exist sama sekali di DB.
- **Hardcode sementara di `src/lib/auth.ts` (`HARDCODE_ADMIN_NIP`) sudah dihapus** — role ADMIN P260054 sekarang murni dari DB.
- **NIP Anggres Saputra diperbaiki**: L240075 → **L240076** (primary key User di-rename via transaksi, 50 baris `MrOutletAssignment` ikut pindah, plus field lain yang mungkin referensi — verified).

## ✅ DONE (25)

| No | Item | Catatan |
|---|---|---|
| 2 | Target sifatnya harus fix, gak bisa naik-turun | Selesai. |
| 3 | C urut historis drafting (periode aktif, gak boleh diedit) | Direstruktur 07-20 jadi per-dokter + toggle Detail expand per kontrak/produk, nama disensor. Bug "tercacah" (nilai kontrak kepakai full-period, bukan diapportion ke kuartal) sudah difix. Sudah ikut ke Excel export. ⚠️ Sisa kecil: growth pelunasan 3 bln terakhir di section ini masih belum dikerjain (lihat #5 catatan). |
| 4 | Hari Praktek per-produk, default otomatis dari level dokter | Selesai. |
| 5 | Growth PSSP berdasarkan value 3 bulan terakhir | Card "Growth PSSP (3 Bln Terakhir)" di LineItemEditor, nampilin growth dari data sales 3 bln terakhir (terpisah dari growth berbasis kontrak PSSP lama). Ditandai selesai oleh stakeholder 07-21. ⚠️ Sisa kecil: growth **pelunasan** 3 bln terakhir khusus di section PSSP Aktif (#3) — konsepnya mirip badge "Pernah PSSP" (#31), scope-nya masih belum dikerjain/dikonfirmasi. |
| 6 | Nama outlet dipanjangin di dropdown | Combobox bisa lebih lebar dari trigger-nya. Sort outlet chain-first (grup di atas, NON CHAIN di bawah) juga sudah jalan — sempat ada bug 07-20: kolom `groupRS` di DB isinya literal string `"NON CHAIN"` (bukan `null`) utk outlet non-chain, jadi kena anggap truthy dan gak ke-sort; sudah difix pakai helper `isChainGroup()` di `LineItemEditor.tsx`. |
| 7 | Target Produk Fokus per area (target kuantitas, bukan cuma counter "1/22") | Card baru "Target Produk Fokus (Kuartal Ini)", pakai mesin `targetCalculation.ts`. Posisi di dekat tombol Export Excel. ⚠️ +~3 detik load time, belum di-cache. ⚠️ Semua keluar 0 di data lokal (OutletSalesMonthly kosong) — perlu dicek di production kalau masih "belum muncul". |
| 8 | Ratio Budget/Target dihilangin dari stat bar | Selesai — tapi ada feedback minta sebagian balik (duplikasi angka di kartu Ringkasan kanan), lihat item #9 (FEEDBACK #A.2). |
| 9 | Summary atasan perlu direview | Kemungkinan besar = FEEDBACK #A (screenshot Detail POA): (A.1) pindah section PSSP Aktif ke atas + stat "PSSP ESTIMASI AKTIF Q4", (A.2) hapus duplikasi Estimasi/Target/Ratio di kartu Ringkasan kanan, (A.3) growth dari Sales Quarter sebelumnya tampil kecil, (A.4) Pengali Nilai R + info visit/bulan di baris ringkasan dokter. ⚠️ Per cross-check 07-20: A.1/A.3/A.4 masih belum dieksekusi, cuma A.2 yang arahnya udah jelas (resolved) tapi belum jalan — kalau ditandai selesai di sini, perlu diverifikasi lagi item mana yang dimaksud "sudah direview". |
| 15 | Narfoz injeksi kelipatan jadi 5 | `konversiPembagi` 1 → 5. |
| 17 | Data diskon sinkron database | "% Diskon (DPL/DPF)" default ke `DiskonKontrak.newOnPi` asli begitu outlet+produk+Periode Awal ke-pilih, tetap bisa diedit manual. Kalau >1 kontrak match (double), dipilih `newOnPi` TERBESAR. Diverifikasi end-to-end (outlet FN000441 + ACETRAM, 2 kontrak overlap 30 vs 33.5 → auto-fill 33.50). 07-20: fallback dummy "10"/"2.5" (diskon/listing fee/entertain) diganti jadi 0 kalau memang gak ada data kontrak sama sekali. |
| 18 | Nilai R Final per dokter di draft | Baris "Nilai R Final" (weighted avg by rencanaTotalBiaya) di Daftar User + kolom per produk di detail expand. ⚠️ Ambiguitas nama field "Nilai R Final" vs "Pengali Nilai R" masih belum diklarifikasi — *"yang dimasukkin di draft itu bukan nilai R akhir, tapi pengali Nilai R"*. Jangan diubah/dihapus sebelum ada keputusan ganti-label vs tambah-field-baru. 07-20: posisi field "Pengali Nilai R" dipindah biar nempel langsung di bawah card "Nilai PSSP" (satu kolom grid yang sama), bukan lagi full-width di bawah seluruh grid. |
| 19 | Data dosis per Produk | **Selesai 07-20** — sumber data ternyata udah ada (`internal/List Product pharos.xlsx`, sheet "Oral Product"/"Injeksi Product"), gak perlu nunggu ABED. Kolom "PROCOD" di file itu ternyata sama persis dgn `Product.kodeProduk` (dikonfirmasi user, diverifikasi 191/192 kode match langsung ke DB). Prisma schema nambah 7 kolom baru (`dosisKekuatanSediaan`, `qtyPerRxPasien`, `lamaPemberianHari`, `jumlahPemberianPerHari`, `bentukSediaan`, `packing`, `indikasi`) via migration `20260720085421_add_product_dosis_zat_aktif_fields`, di-sync pakai script baru `scripts/syncProductZatAktifDosis.ts` (194 produk ke-update). Ditampilin sebagai teks abu-abu "Referensi: ..." di bawah field "Jml Produk ST / Resep" di `LineItemEditor.tsx`. Sisa: auto-fill produk kompetitor dari data survey — belum dikerjain. |
| 20 | Pengali Nilai R per produk, bukan totalan per dokter | Awalnya cuma fix unit `%`→`x`; versi "default dokter" dihapus total — sekarang murni per-produk. |
| 21 | Notes tambahan "untuk perkuat argumen pengajuan" saat Ajukan ke Atasan | Selesai. |
| 23 | Halaman approval + jumlah kunjungan MR ke user | ⚠️ **Perlu diverifikasi ulang** — per cross-check kode 07-20 gak ketemu implementasinya (halaman approval yang ada belum nampilin jumlah kunjungan MR). Ditandai selesai sesuai info terbaru dari stakeholder; tolong cross-check langsung ke halaman approval di app kalau perlu dipastikan lagi. |
| 24 | Satuan jual per box / Estimasi Produk Fokus | Card "Estimasi Produk Fokus" (+ stat bar atas) selesai. ⚠️ Per cross-check 07-20: detail per-produk dgn tanda warna kategori (fokus/red ocean/low hanging fruit) di tabel detail masih tabel polos, belum ada pewarnaan di situ — kalau ini dianggap "final", berarti requirement warna kategorinya cuma dianggap perlu di product picker (sudah ada, lihat #32), bukan di tabel detail. |
| 28 | Summary produk belum diajukan tapi rekomendasi PM (kuning/oranye) | Terjawab lewat item #31. |
| 29 | Sidebar "Produk Fokus PM Belum Diajukan" per spesialis | Muncul di sidebar kanan form MR begitu spesialisasi dokter ke-detect, isinya produk tier-0 yang belum ada di produkList. |
| 30 | Informasi Zat Aktif di product picker | **Selesai 07-20** — bareng #19, dari `internal/List Product pharos.xlsx`. `Product.zatAktif` (udah ada di schema tapi sebelumnya selalu `null` dari `syncProducts.ts`) sekarang keisi via `scripts/syncProductZatAktifDosis.ts`. Ditampilin di sublabel product picker (`buildProductOptions` di `LineItemEditor.tsx`, ikut ke-filter pas search) + di info bar setelah produk dipilih. |
| 31 | Kriteria Produk "Produk Pernah Di PSSP" | Badge "Pernah PSSP · Pelunasan 3 Bln XX%" di product picker, warna hijau ≥80% / kuning 40-79% / merah <40%, discope by histori PSSP dokter+outlet 3 bulan terakhir. |
| 32 | Warna Low Hanging Fruit (Ada Sales = Oren, Tidak Ada Sales = Kuning) | Pakai field `OutletProductKriteria.kategori`. 07-20: badge ini diubah jadi dot warna polos (gak nampilin teks "Low Hanging Fruit" lagi) di product picker — badge kriteria lain (standarisasi/kompetisi) tetap pakai teks seperti biasa. |
| 33 | Sinkron dgn kunjungan & PSSP aktif (kelihatan mana yg dikunjungi/tidak) | ⚠️ **Perlu diverifikasi** — gak ketemu fitur cross-reference kunjungan↔PSSP-aktif di kode per pengecekan 07-20. Kalau maksudnya beda dari yang saya cek, tolong dijelaskan lokasinya di app biar bisa di-follow up. |
| 34 | Detail periode untuk pelunasan yang buruk | Bagian dari panel Histori PSSP di form MR (nama RS/outlet tiap kontrak + filter ke outlet yang lagi dipilih) — selesai 07-20. |
| 35 | Detail running rate 3 bulan terakhir | Bagian dari panel Histori PSSP di form MR (running rate pace pelunasan utk kontrak aktif) — selesai 07-20. Sisa dari panel ini: sumber data "Pak Eko"/item NON ESTIMASI, masih CLARIFY (lihat #38-area / bagian bawah). |
| 37 | Tim NSM Ex-Hospinet gak bisa login ke sistem | Kemungkinan besar = fix "Case-insensitive login" (selesai). ⚠️ Gak ada jejak eksplisit "Hospinet" di kode/commit — kalau masih ada laporan gagal login dari tim ini, perlu dicek kasus spesifiknya (kemungkinan nip/username beda kapitalisasi atau isu lain). |
| 44 | Diskon: pakai DPL kalau ada, fallback ke history diskon kalau gak ada | **Selesai & data ter-import 07-21** — model baru `DiskonHistory` (kodePI+kodeProduk → rata-rata tertimbang "% Total Diskon"), diisi dari `internal/08062026 Data Diskon All Product Jan-Apr'26.xlsx` (sheet "Raw Data", 490.611 baris, file 196MB — dibaca pakai ExcelJS streaming reader biar gak OOM) via script baru `scripts/importDiskonHistory.ts` → **201.963 pasangan outlet+produk** ter-agregasi (periode sumber 202601-202604). "Item Kode" di file ini dikonfirmasi match langsung ke `Product.kodeProduk` (dicek manual: "000070"→NEBACETIN, "013390"→ACETRAM, sesuai). `resolveDiskonPctWithHistory()` di `LineItemEditor.tsx` coba DPL (`DiskonKontrak`) dulu, baru fallback ke history kalau DPL gak ada — DPL tetap prioritas, history cuma fallback sesuai arahan. |
| 45 | Kalau ASM/SM vacant, approval langsung ke NSM (skip level) | **Selesai 07-21** — `resolveNextHolder()` (`poaWorkflow.ts`) diubah dari fixed-hop (`.reportsTo.reportsTo.reportsTo`) jadi jalan-jalan menyusuri chain berdasarkan **role**, jadi otomatis skip level yang vacant. Data-nya juga dibenerin: `importStrukturVerifiedKAM.ts` sekarang skip-link `nipAtasan` pas import (sebelumnya dibiarkan `null` kalau ASM/SM vacant, sekarang lompat ke level di atasnya) — hierarki lama di-backfill ulang. |
| 46 | ASM bisa input POA untuk tim yang vacant | **Selesai 07-21** — tapi scope-nya diperbaiki dari implementasi pertama: bukan "subordinate ASM/SM/NSM kosong semua", tapi **per outlet** — kalau outlet tertentu MR & ASM-nya vacant, SM yang bisa input POA khusus utk outlet itu (kalau SM juga vacant, baru NSM), gak peduli tim lain di bawah SM itu penuh atau nggak. Field baru `Outlet.coveredByNip`/`coveredByRole` (dihitung pas import, siapa yang efektif cover outlet itu). `canCreatePoa()` (`authz.ts`), `getOutletsByUser()` (`masterData.ts`, scope outlet picker ke outlet yg di-cover doang), `canView`/`canEdit`/`getVisiblePoaFilter` (generalisasi "pemilik POA selalu bisa akses", gak lagi hardcode role MR), tombol submit di dashboard & Detail POA (gak lagi hardcode `isMR`) — semua disesuaikan. |
| 54 | Satuan Produk Albusmin Box→Kapsul | **Selesai 07-27** — sesuai konfirmasi user, cuma label yang diubah: field `Product.satuan` utk kodeProduk `0304507` ("ALBUSMIN 3 BLISTER @ 10 KAPSUL") di-update langsung di DB dari `"BOX"` → `"KAPSUL"`. `hna` dan `konversiPembagi` (30) sengaja TIDAK direcalculate (user pilih opsi "cuma ganti label, HNA tetap") — jadi HNA yg tersimpan masih harga per-box lama, cuma labelnya sekarang bilang per-kapsul. 6 kode Albusmin lain di DB `satuan`-nya kosong (`"—"`), gak disentuh. |
| 56 | Perubahan Istilah: GROWTH PSSP → GROWTH Pelunasan | **Selesai 07-27** — cuma 2 titik ketemu di seluruh `src`: label card "Growth PSSP (3 Bln Terakhir)" → "Growth Pelunasan (3 Bln Terakhir)" + komentar kode, keduanya di `LineItemEditor.tsx`. |
| 62 | Hapus Labeling Pelunasan | **Selesai 07-27** — `computeLabelCustomer()` di `LineItemEditor.tsx` sekarang return `"Akan Selesai"` / `"Pernah PSSP"` polos (tanpa qualifier) kalau pelunasan <80%, bukan lagi `"...Pelunasan Buruk"`. Label `"...Pelunasan Bagus"` (≥80%) TIDAK dihapus/diubah — cuma yang "Buruk" yang dihilangkan sesuai request. `LabelCustomerBadge` disederhanain, cabang warna merah (`isBad`) dihapus karena udah gak ada label yang match. |
| 60 | Label Retensi Dokter | **Selesai 07-27** — badge oranye "Retensi" di `ContractCard` (panel Histori PSSP, `LineItemEditor.tsx`), muncul kalau kontrak masih aktif DAN `prdAkhir`-nya jatuh di salah satu bulan quarter kalender berjalan (helper baru `currentQuarterMonths()`, pakai `quarterToMonths()` dari `quarterUtils.ts`). Periode berakhir (`prdAwal – prdAkhir`) sendiri sebenernya udah ditampilin dari sebelumnya, cuma badge-nya yang baru. |
| 61 | Sorting Dokter PSSP | **Selesai 07-27** — dropdown pilih dokter (`customerOptions` di `LineItemEditor.tsx`) sekarang sort utama berdasarkan jarak (dalam bulan) `latestPrdAkhir` kontrak PSSP terakhir ke akhir quarter kalender berjalan (makin dekat/mendesak, makin atas), sort-by-pelunasan-terbaik (2026-07-24) jadi tie-breaker kedua. Field baru `latestPrdAkhir` ditambah ke `PsspStatusByCustomer` (`customer.ts`). Dokter yang belum pernah PSSP tetap tenggelam ke bawah. |
| 52 | Sidebar "Produk Rekomendasi" (rename dari "Kriteria Produk") | **Selesai 07-27** — tab hijau di-rename jadi "Produk Rekomendasi" (2 titik teks: pill switcher + collapsed edge tab). Urutan section di `KriteriaProdukPanel` diubah sesuai request: **Pernah PSSP** (sort pelunasan terbaik, sudah ada sebelumnya) → **Produk Fokus PM** → **Produk Survey** (BARU — fetch `getSurveyRekomendasiByOutlet` langsung di panel ini, sesuai konfirmasi user). Section "Listing Corporate - Ada/Tidak Ada Sales" TETAP ada, ditaruh setelah 3 section itu (user pilih "tetap ada" bukan "hapus"). Tab terpisah "Data Survey" (oranye) TETAP ada apa adanya (user pilih "biarkan terpisah") — jadi data survey sekarang sengaja muncul di 2 tempat (tab sendiri + section baru ini), bukan bug. |
| — | Download Excel: angka muncul tanpa nunggu status APPROVED | **Audit 07-27, ternyata udah lama beres** — baik `export/[id]/route.ts` maupun `export/team/route.ts` gak ada status-gating apapun di angka (Estimasi/PSSP/budget dihitung & ditampilin langsung dari `PoaLineItem`, gak peduli status). Komentar eksplisit di `export/team/route.ts:84` konfirmasi ini disengaja ("this team rekap includes every status"). |
| — | Entertain Default: 1 | **Audit 07-27, ternyata udah lama beres** — `emptyProdukEntry()` (`LineItemEditor.tsx:118`) dan saat pilih produk (`:745`) set `persenEntertain: "1"`. Komentar di kode: "0 dibaca sebagai 'field kelupaan diisi' di praktiknya, jadi 1 dipilih sebagai default non-zero yang disengaja." Ini keputusan TERPISAH dari fallback diskon/listing fee yang dijadiin 0 (lihat item #17). |
| — | Approval Backdoor NSM | **Audit 07-27, ternyata udah lama beres** — `authz.ts:254-264` & `poaWorkflow.ts:235-260`: NSM bisa langsung approve POA ke `APPROVED_BY_NSM` dari status manapun (`SUBMITTED_TO_ASM/SM/NSM`), skip cek `currentHolderId`/urutan chain — asal POA itu masih di dalam subtree-nya NSM tsb. Komentar kode kutip permintaan bisnis 2026-07-23 langsung. |
| — | Jumlah user per spesialisasi + sorting dropdown by user terbanyak | **Audit 07-27, ternyata udah lama beres** — `specOptions` (`LineItemEditor.tsx`) ngitung jumlah customer per label spesialisasi, kasih sublabel "N dokter terdaftar", `.sort((a,b) => b._count - a._count)` descending. |
| — | Hide fitur "Target Produk" untuk role NSM | **Audit 07-27, ternyata udah lama beres** — `Sidebar.tsx`: nav item "Target Produk" di-gate `roles: ["ADMIN"]` doang. Komentar kode: "NSM temporarily hidden from sidebar (2026-07-24) — re-add 'NSM' to bring it back." |
| — | Indikator status dokter (Pernah PSSP Baik/Buruk) + % pelunasan terakhir saat pemilihan | **Audit 07-27, ternyata udah lama beres** — sama seperti item #61 di atas: `psspStatusByCust` + `customerOptions` (`LineItemEditor.tsx`) bikin `tag2 = "Pernah PSSP · Pelunasan Terakhir X%"` dgn warna hijau/kuning/merah di threshold 80%/40%, muncul di dropdown pilih dokter SEBELUM MR pilih siapapun (2026-07-24). |
| — | Layar Dashboard BY MR gak dilimit 10, bisa scroll | **Audit 07-27, ternyata udah lama beres** — `dashboard/page.tsx`: default `pageSize = 25` (dari query param `?size=`), dan `?size=all` kasih `pageSize = null` (unlimited). Gak ada `slice(0, 10)`/limit-10 hardcoded di manapun di file ini. |
| — | Kriteria Produk: "Produk standarisasi - tidak ada sales" → "Produk sudah listing corporate - tidak ada sales" | **Audit 07-27, ternyata udah lama beres** (2026-07-24) — `formatKriteriaLabel()` (`LineItemEditor.tsx:30-31`) rename semua tampilan "Terstandarisasi" jadi "Listing Corporate" di badge picker produk; data mentah `kriteriaBaru` di DB TETAP raw "Terstandarisasi" (cuma matching-nya yang harus tetap pakai raw value, cuma teks yang ditampilin yang diganti). |
| 26 | Format data survei SFE ke sistem + Sidebar Data Survey | **Pindah dari ON-PROSES ke DONE 07-27, audit ternyata udah lama beres penuh, bukan cuma rencana** — `scripts/importSurveyRekomendasi.ts` beneran import `internal/17062026 Data Rekomendasi Final.xlsx` ke model `SurveyRekomendasi` per (dokter, outlet, produk), dgn validasi kodeDokter/kodeRS ke DB. `SurveyDataPanel` (`LineItemEditor.tsx`) fetch data ini lewat `getSurveyRekomendasiByOutlet` (`customer.ts`), render kartu per produk rekomendasi + badge kompetitor, sudah full-function (loading/empty state dll), nempel di tab sidebar oranye "Data Survey". ⚠️ Caveat: stakeholder minta "split 2 tabel" (produk rekomendasi vs kompetitor) — yang ada sekarang cuma SATU list kartu, dgn kompetitor sebagai badge kecil NESTED di dalam tiap kartu, bukan tabel terpisah — kalau maksudnya harus 2 tabel visual yang benar2 terpisah, ini masih perlu kerjaan tambahan. |
| 36 | Struktur AP.K24 Matraman (Jakarta Timur) → harusnya kategori B | **Diverifikasi 07-27, ternyata udah benar** — query langsung ke DB production: outlet `F1011627` ("K-24 MATRAMAN, APT") field `kategori` = **"B"** persis kayak yang diminta. Gak perlu fix apapun, cuma emang belum pernah dicek manual sebelumnya. |
| 64 | Cancel Approved NSM | **Selesai 07-27** — NSM bisa membatalkan approval yang udah dia berikan sendiri, POA balik ke status `REVISI` (sama persis konsekuensinya kayak reject: wajib direvisi ulang, version number naik) — sesuai konfirmasi user. Enum baru `AuditAction.CANCEL` (migration `20260727150000_add_audit_action_cancel`). Fungsi baru `cancelApprovedByNsm()` (`poaWorkflow.ts`, pola sama persis `rejectPoa()`) + `canCancelApproved()` (`authz.ts`, gate: role NSM DAN status genuinely `APPROVED_BY_NSM` — beda dari `canApprove` yang holder-gated, karena `currentHolderId` udah `null` begitu fully approved) + `cancelApprovedByNsmAction()` (`poa.ts`, wajib isi alasan sama kayak reject). UI: card "Batalkan Approval" baru muncul di `poa/[id]/page.tsx` kalau `isFullyApproved && userCanCancelApproved`, persis di bawah banner "sepenuhnya disetujui" — sudah diverifikasi visual via Playwright (login sebagai NSM asli `P030966`, POA `f26d2786-...`, tombol + textarea alasan muncul benar). Label Riwayat Aktivitas: "membatalkan approval (kembali ke Revisi)". |
| 47 | Matriks Summary Per Outlet: kolom baru | **Selesai 07-27** — tab "Per Outlet" di `/summary` (`summary/page.tsx` + `TerritoryTable.tsx`) sekarang punya 8 kolom baru: **Estimasi Aktif+Pengajuan** (gabung `estimasi` pengajuan existing + `estBaris` dari kontrak PSSP aktif via `getActivePsspByOutlets`, di-groupby `kdOutlet`), **User PSSP (Aktif+Estimasi)** (union kdCust aktif ∪ kodeCust draft), **Variasi Produk (Fokus/Non-Fokus)** (format "X/Y"), **Budget** & **Cost Ratio** (formula existing dari `poa/[id]/page.tsx`/`export/team/route.ts`: budget = Σ rencanaTotalBiaya×(psspPct+discPct+entPct), ratio = budget/estimasi — TIDAK pakai denominator aktif+pengajuan, tetap konsisten sama definisi lama), **Sales Aktif (2026)** (real data dari `OutletSalesValueMonthly`, periode ≥202601 — **9.289 baris data asli tersedia**, diverifikasi bukan nol semua), **Estimasi Per User**, **Listing Fee** (agregat baru `ListingFeeKontrak.value` groupBy `kdOutlet` — belum pernah ada agregat per-outlet sebelumnya). Diverifikasi render di browser (Playwright, login ADMIN) — semua kolom nampilin angka asli, contoh baris pertama: Estimasi Rp135,4 Jt, Budget Rp20,3 Jt, Cost Ratio 15.0%. ⚠️ **Perf note**: utk role ADMIN (company-wide, semua MR) halaman ini jadi lambat — **58-74 detik** load time saat ditest, jauh lebih lambat dari sebelumnya krn agregat baru (active PSSP + sales + listing fee) query across SEMUA outlet company-wide. Untuk NSM/SM/ASM (scope lebih kecil, subtree sendiri) kemungkinan jauh lebih cepat, tapi belum ditest langsung — perlu dicek kalau dirasa lambat di production. |
| 48 | Matriks Summary Per Produk (rename dari "Summary Detail Produk") | **Selesai 07-27** — tab "Per Produk" di `/summary` sekarang punya kolom: **Status** ("Fokus"/"Non-Fokus", pakai `getAllPakets()` sesuai konfirmasi user — bukan `getProductTier()` yg spesialisasi-aware), **Estimasi Aktif+Pengajuan** (PSSP aktif di-match by **nama produk ternormalisasi**, BUKAN kodeProduk — krn `PsspKontrak.kdProduk` beda namespace sama `Product.kodeProduk`, "Procode ≠ Item Kode", sesuai konvensi existing di `computeOldEstPerMonth`), **User Aktif PSSP** (distinct kdCust dari kontrak aktif match nama produk), **Budget**/**Cost Ratio** (formula sama kayak #47), **Sales Aktif (2026)** (DIDERIVE dari `OutletSalesMonthly.qty × Product.hna` — **bukan** angka Rupiah riil observed kayak punya outlet, karena emang gak ada model sales value per-produk; qty asli tersedia **51.227 baris** via `itemKode` yang match langsung ke `Product.kodeProduk`), **Sales Per User**, **AVG Pasien Per User** & **AVG ST per Pasien** (formula `qtyProdukResep / jumlahPasienHari` sesuai konfirmasi user, dari field `PoaLineItem.jumlahPasienHari`/`qtyProdukResep` — nullable, di-filter dulu sebelum di-average). Diverifikasi render di browser — baris pertama contoh: INTRIX 1G INJEKSI, Status Fokus, Estimasi Rp4,2M, User Aktif PSSP 159, Sales Aktif Rp6,0M, Sales Per User ≈Rp37,9Jt (matematisnya konsisten: 6M/159≈37,9Jt). AVG Pasien/AVG ST tampil "—" utk baris yg field `jumlahPasienHari`/`qtyProdukResep`-nya belum diisi (expected, field opsional). |

## 🟡 ON-PROSES (3)

| No | Item | Catatan |
|---|---|---|
| 1 | Struktur baru *(top urgent!!)* | **Progress besar 07-20/07-21**: import `internal/Struktur Verified Part KAM.xlsx` (sheet "ALL", 7334 outlet, NIP langsung per level GM/NSM/SM/ASM/SPV/MR) via `scripts/importStrukturVerifiedKAM.ts` — sekarang **365 User** di-upsert + **350 hierarki** (nipAtasan) ke-wire, **3799 outlet dapat assignment MR baru/ter-override**, plus `Outlet.coveredByNip`/`coveredByRole` per outlet (3799 MR normal · 808 vacant→ASM · 383 vacant→SM · 2344 vacant→NSM · **0 gak ada siapa-siapa**). **07-21 fix "SPV is MR"**: kolom SPV & MR di file itu ternyata dua nama utk level yang sama (cuma salah satu keisi per baris) — `resolveCoverage()` dan `MrOutletAssignment` sebelumnya cuma cek kolom MR doang, jadi ~1824 outlet yang cuma keisi kolom SPV (MR-nya kosong) kesalah-anggap "MR vacant" dan ke-escalate ke ASM/SM/NSM. Dibenerin pakai `effectiveMrNip(r) = r.mrNip ?? r.spvNip` di kedua tempat itu — coverage "MR" naik dari 1975→3799, dan 197 outlet yang tadinya genuinely gak ke-resolve sekarang semua ke-cover. Dua bug data kolom "NIP NSM" ditemukan & dibenerin via hardcode override di script: **"EKA"** (07-20, isinya rangkaian kode acak per baris — NIP asli `P260205`) dan **"DODY ALWARDY"** (07-21, kosong di semua 580 barisnya — NIP asli `P250442`, tanpa ini 197 outlet di bawahnya gak punya fallback sama sekali). Sisa: 2685 outlet msh gak ada MR yang resolve (assignment lama dipakai) + 850 outlet NIP MR terisi tapi nama placeholder (sisa data lama) — ini genuinely keputusan bisnis manual, bukan masalah data lagi. Kasus spesifik "AP.K24 Matraman Jakarta Timur → harusnya kategori B" **sudah diverifikasi 07-27** — dicek langsung ke DB production, outlet `F1011627` kategori-nya emang udah "B" (lihat #36 di tabel DONE). ⚠️ Stakeholder nambahin ✅ di sebelah item ini tapi labelnya masih "(ON-PROSES)" — saya tetap taruh di sini karena progress-nya emang belum 100%, tapi kalau maksudnya udah dianggap cukup, tinggal bilang aja. **Klarifikasi 07-27**: stakeholder juga nyebut "SPV dirubah jabatanya menjadi SPV dari MR" sebagai item terpisah — audit kode nemuin ini SAMA MEKANISMENYA sama fix "SPV is MR" di atas (`effectiveMrNip`), plus field `User.jabatan` (schema) yang emang udah ada buat override display-only title "SPV" walau role sebenarnya MR. Bukan role/enum baru — gak ada `SPV` di `enum Role` (`schema.prisma`). |
| 22 | Notes alasan reject dari atasan | Tombol Reject eksplisit sudah ada, wajib isi alasan, tercatat di Riwayat Aktivitas — kalau masih dianggap on-proses, kemungkinan ada scope tambahan yang diminta (mis. notifikasi ke MR, atau tempat tampil lain) yang perlu diperjelas. |
| 25 | History Visit sebelumnya (warna abu-abu, gak perlu tabel) | Placeholder "Hari Praktek/Bln" sudah selesai. Sisa: histori jumlah visit (angka, bukan cuma teks status) ditaruh sebelah field "Rencana Visit/Bulan", styling teks abu-abu aja sesuai arahan. ⚠️ Audit 07-27: stakeholder klaim ini DONE ("Masukan History Visit sebelumnya Proses Input") tapi gak ketemu jejaknya di kode sama sekali (gak ada grep match utk "bulan lalu"/"riwayat visit"/angka historis di sekitar field "Rencana Visit/Bulan") — tetap ditaruh ON-PROSES sampai ada klarifikasi/link ke lokasi di app. |

## ❓ NEED CONFIRMATION (8)

| No | Item | Pertanyaan/status |
|---|---|---|
| 10 | PSSP/KPDM per outlet | *"seharusnya kpdm dan dpl itu per outlet, dan sudah teridentifikasi dari pengisian awal"* — belum ada keputusan/implementasi. ⚠️ Audit 07-27: stakeholder klaim "Pilihan Dropdown USER, KPDM" DONE — grep "KPDM" di seluruh `src/` cuma ketemu di komentar `persenPsspKpdm` (field budget %, gak ada hubungannya sama dropdown per-outlet ini). Gak ada dropdown/field KPDM apapun di kode. Klaim DONE-nya gak match kode, tetap NEED CONFIRMATION. |
| 11 | Pilihan PS / SP / peremajaan | Kemungkinan terkait dropdown "Jenis PSSP" yang udah dikerjain (full-stack selesai tapi di-hide sementara dari UI). Belum jelas apakah cukup dropdown simpel atau perlu level otomasi. ⚠️ Audit 07-27: stakeholder JUGA klaim "Pilihan Dropdown PS/SP" DONE di list terpisah — tapi kode `LineItemEditor.tsx` masih `{false && (...)}` yang wrap dropdown Jenis PSSP ini (comment: "hidden for now per request... field + logic kept intact for a quick re-enable later"). Klaim DONE-nya kontradiksi kode DAN kontradiksi NEED-CONFIRMATION list stakeholder sendiri (item ini masih ada di situ juga) — kemungkinan salah centang di sisi mereka. Tetap NEED CONFIRMATION. |
| 12 | Konsep tabungan | Desain belum dipikirin. |
| 13 | Ratio target 140% dihilangin | Masih ada di kode (`DraftChecklist.tsx`: `ratioEst >= 140`). Perlu dipikirin rekomendasi % pengganti / formulanya. |
| 14 | Monitoring POA lama & sebelumnya | API yang relevan namanya **"Nexus"** (api-nexus.pharos.id) — bukan "Exodus". Sudah dieksplor, lihat catatan di #1. |
| 16 | Listing fee otomatis by outlet | `ListingFeeKontrak` data sudah diimport, tapi belum jelas apa "pembagian otomatis per outlet" ini udah kepakai di kalkulasi POA atau masih manual — perlu konfirmasi formula. |
| 27 | Sisa budget ditampilkan value tahunan | Belum dikerjain — perlu konfirmasi formula (budget historis?). |
| 38 | Historis Kunjungan By MR by Customer (akumulasi 3 bulan terakhir) | Item baru dari stakeholder, konfirmasi ke Pak Fakhri (via Anthony SFE Pharos). Belum ada implementasi/desain. ⚠️ Item ini juga udah di-drop dari NEED-CONFIRMATION-7 versi terbaru stakeholder (gak disebut sama sekali, bukan di DONE maupun ON-PROSES) — statusnya jadi gak jelas, saya PERTAHANKAN di sini sampai ada klarifikasi eksplisit. |

## ⬜ BELUM DIKERJAKAN — item baru 07-21 (#39-43)

*(Gak ada tag status dari stakeholder di 5 item ini, jadi ditaruh di sini apa adanya. Beberapa udah saya cross-check cepat ke kode, ada catatan; sisanya belum sempat digali.)*

| No | Item | Catatan |
|---|---|---|
| 39 | Check COUNT OF PRODUCT di master Produk, check HNA (harus ikutin kenaikan harga per Juli) | Audit data — belum dicek. Perlu tau sumber "harga baru per Juli"-nya dari mana buat dibandingin ke `Product.hna` yang ada sekarang. ⚠️ Terkait: audit 07-27 nemuin `Product.nilaiRPersen` itu snapshot manual dari `scripts/syncNilaiR.ts` (import sekali dari file "Nilai R.xlsx"), BUKAN dihitung ulang otomatis dari `hna` yang berjalan — jadi kalau HNA naik tapi `syncNilaiR.ts` gak di-rerun, %-nya jadi stale. Relevan buat item ini juga. |
| 40 | Check Struktur Hospinet / KAM | Import `Struktur Verified Part KAM.xlsx` (lihat #1) cuma cover divisi **KAM** — Hospinet masih belum ke-cover di struktur org (org hierarchy/outlet coverage tetap gak ada data Hospinet). **07-21 progress parsial**: `internal/customer_pssp_hospinet.xlsx` di-import via `scripts/importPsspHospinet.ts` — ini ngasih customer roster + snapshot PSSP level-customer utk Hospinet (lihat #42), tapi BUKAN struktur org/outlet-ownership Hospinet, jadi #40 masih belum full selesai. |
| 41 | Fitur Log-out di Mobile | ✅ **Selesai 07-21** — koreksi catatan sebelumnya: tombol "Sign out" ternyata SUDAH ADA di `Sidebar.tsx` (footer sidebar, form POST ke `/api/auth/logout`), bukan "belum ada sama sekali". Bug asli: di mobile, drawer sidebar-nya pakai `h-screen` (`100vh`) sekaligus `position: fixed` + `inset-y-0` — kalau `top`/`bottom`/`height` ketiganya keisi di elemen fixed, `height` menang atas `bottom` (CSS2.1 §10.6.4), jadi begitu address bar browser mobile muncul (visible viewport < 100vh), bagian bawah drawer (termasuk tombol Sign out) ke-dorong ke bawah layar yang gak keliatan/gak bisa diklik. Fix: `h-screen` → `h-dvh` (dynamic viewport height, ngikutin viewport asli yang keliatan). |
| 42 | Check Database Customer terbaru | **07-21 progress**: import `internal/customer_pssp_hospinet.xlsx` (2376 baris) via `scripts/importPsspHospinet.ts` — data ini flat (nama+spesialisasi+outlet+status+value PSSP/pelunasan/RR agregat, TANPA nomor kontrak/produk/breakdown bulanan), jadi sengaja dibikin model baru `PsspHospinetSnapshot` (mirip `DiskonHistory` vs `DiskonKontrak`, gak dipaksa masuk `PsspKontrak`). Hasil: **1887 Customer baru** + **1919 CustomerOutlet** + **1919 PsspHospinetSnapshot** (32 nama match ke Customer existing, 357 baris skip krn spesialisasi kosong, 64 skip krn outlet gak ada di tabel `Outlet`). Ditampilin di sidebar "Histori PSSP" sbg fallback card pas gak ada histori `PsspKontrak`. ⚠️ Vocab spesialisasi Hospinet ("Dokter Umum" dst) beda dari 16 kategori kurasi PM (`SPESIALISASI_PM_LABEL`) — cuma ~14% (295 baris: Anak/Kandungan/Penyakit Dalam/Saraf/Paru/Psikiatri/Ortopedi/Bedah Umum/Anestesi) yang ternormalisasi ke kategori kurasi, sisanya (termasuk "Dokter Umum" 79%) tetap raw value krn gak ada bucket GP di list kurasi — customer2 ini gak muncul di dropdown spesialisasi form Tambah POA. Audit Customer database yang lebih luas (bukan cuma Hospinet) masih belum dikerjain. |
| 43 | Optimalisasi Versi Mobile | Belum dikerjain — scope-nya luas (responsive layout dll), belum di-breakdown jadi task konkret. ⚠️ Stakeholder klaim ini DONE ("Optimalisasi Versi Mobile ✅") di list DONE mereka — tapi gak ada scope/task konkret yang bisa di-cross-check ke kode (item ini dari awal emang belum di-breakdown). Kalau ada kerjaan spesifik yang udah dianggap "selesai", tolong disebutin biar bisa diverifikasi. |

## ⬜ BELUM DIKERJAKAN — item baru 07-27 (#47-65)

*(16 item baru dari stakeholder tgl 2026-07-27 (#47-62), ditambah 3 lagi susulan di hari yang sama (#63-65) — #63/#64/#65 dari revisi/tambahan terbaru stakeholder. #52/#54/#56/#60/#61/#62 (low-hanging fruit) dan #47/#48/#64 (3 fitur besar) sudah dikerjain 07-27, pindah ke tabel DONE di atas. Sisanya belum dicek ke kode, ditaruh apa adanya sesuai request.)*

| No | Item | Catatan |
|---|---|---|
| 49 | Input Data Target: top-down | Sifat pengisian top-down dahulu. |
| 50 | Halaman Approval By Personil: kolom wajib | Wajib menampilkan Estimasi (Aktif + Baru), Budget, dan Cost Ratio. Kemungkinan overlap/subset dari #63 (Matriks Summary Per Personil) yang lebih lengkap — perlu diperjelas apakah ini 2 halaman beda atau 1 fitur yang sama. |
| 51 | Fitur Sorting pada Summary | Tambahkan kontrol/pilihan sorting, default sort berdasarkan GAP tertinggi terhadap ESTIMASI sebelumnya. |
| 53 | Sorting Data Survey | Diurutkan berdasarkan Produk Fokus PM dulu, lalu Potensi Terbesar. |
| 55 | Download Excel: sheet Summary Per Outlet | Tambahkan sheet baru "Summary Per Outlet" di file Excel export. |
| 57 | Summary Bawah Produk (Form POA): Growth PSSP + Growth Pelunasan | Tambahkan info Growth PSSP dan Growth Pelunasan di bagian summary bawah tabel produk saat pengisian Form POA. |
| 58 | Growth Estimasi per Produk | Tampilkan persentase growth Estimasi POA vs Pelunasan Sebelumnya kalau POA sudah terisi. |
| 59 | Warning Logic Growth Estimasi | Growth ≤ 0: warning "Kenapa malah lebih kecil dan belum intensifikasi". Growth > 0: warning/apresiasi "Selamat sudah intensifikasi, pastikan estimasinya tepat". Terkait #58. |
| 63 | Matriks Summary Per Personil | **Item baru 07-27** — Jumlah Outlet, Jumlah User PSSP Aktif, Variasi Produk (detail Fokus & Non-Fokus: Target, Estimasi, Ach%, Jumlah User), Sales Aktif, Target, Ach%, Budget, Cost Ratio. Kemungkinan overlap sama #50 (Halaman Approval By Personil) — lihat catatan di situ. |
| 65 | Lock Edit Logic | **Item baru 07-27** — kunci akses edit POA begitu atasan udah Approve atau Edit. Perlu diperjelas: siapa yang ke-lock (cuma pemilik POA/MR, atau semua level di bawah atasan yang approve itu?), dan apakah ada mekanisme unlock/override selain reject. |

## ⚠️ Diskrepansi klaim stakeholder vs kode (audit 07-27)

*(Hasil audit kode 07-27 buat cross-check tracker lengkap stakeholder — 20 klaim "DONE"/status ditelusuri; 8 CONFIRMED dan udah dipindah ke tabel DONE di atas dengan tag "Audit 07-27". Sisanya, 2 PARTIAL dan beberapa gak ketemu jejak kodenya, dicatat di sini biar gak hilang tapi juga gak salah ditandai selesai.)*

**PARTIAL (ada implementasinya, tapi gak persis sesuai klaim):**
- **"Data Pelunasan PSSP menggunakan perhitungan VB"** — "VB" ternyata cuma nama file sumber (`internal/PSSP VB Horizontal dan Vertikal.xlsx`, sheet "PSSPVBHor"/"PSSPVBVer"), bukan istilah metode perhitungan. `scripts/importPsspKontrak.ts` cuma import sheet "Hor" (sheet "Ver" sengaja gak dipakai, redundant). Perhitungan pelunasan % sendiri (di `computePelunasanPct`/`export/[id]/route.ts` dan `LineItemEditor.tsx`) tetap logic yang sama kayak sebelumnya (estBaris/totalLunas/cUrut) — gak ada logic baru yang "berbasis VB".
- **"% NILAI R: Perhitungan dipastikan sudah mengikuti kenaikan harga terbaru"** — `nilaiRPersen` itu snapshot manual import (`scripts/syncNilaiR.ts`, sekali jalan dari "Nilai R.xlsx"), BUKAN dihitung ulang otomatis dari `Product.hna` yang berjalan. Kalau HNA naik tapi script ini gak di-rerun, %-nya stale. Lihat juga #39 di atas.
- **"Peletakan data survey (split 2 tabelnya dengan produk kompetitor)"** — yang ada sekarang SATU list kartu per produk rekomendasi, dengan produk kompetitor sebagai badge kecil NESTED di dalam tiap kartu (`SurveyDataPanel`/`KriteriaProdukPanel` di `LineItemEditor.tsx`) — bukan 2 tabel visual yang benar-benar terpisah. Kalau maksudnya harus 2 tabel beneran, ini masih perlu kerjaan tambahan.

**NOT FOUND (diklaim DONE, tapi gak ketemu jejak kode/data sama sekali):**
- **"Check data pricil untuk dimasukin ke specialis"** — grep "pricil" di seluruh repo nihil. Kemungkinan ini task manual di luar kode (data entry langsung di admin panel/DB), gak ada artifact yang bisa diverifikasi.
- **"Check Produk Fokus PM (apakah sudah bener datanya sesuai dengan specialis)"** — sama, gak ada script/audit artifact yang cocok di `scripts/`. Kemungkinan task manual/audit spreadsheet di luar repo.
- **"Dropdown USER, KPDM"**, **"Masukan History Visit sebelumnya"** — lihat masing-masing di tabel NEED CONFIRMATION #10 dan ON-PROSES #25 di atas, keduanya diklaim DONE oleh stakeholder tapi audit kode gak nemu buktinya sama sekali. (Catatan: "Struktur AP.K24 Matraman → kategori B" awalnya juga masuk sini, tapi setelah dicek langsung ke DB production ternyata BENERAN udah "B" — pindah ke DONE #36, bukan diskrepansi.)

---

## 🔧 Item tambahan (tracked internal, di luar 46-list stakeholder di atas)

*(Ini item yang sebelumnya cuma ditrack internal — gak ada nomor asli dari daftar stakeholder — tapi tetap relevan buat histori kerjaan.)*

### ✅ Selesai

| Item | Catatan |
|---|---|
| Sort dropdown produk (produk pernah-PSSP duluan) + qty & star breakdown di "Total Semua Produk" | Selesai. |
| Riwayat Aktivitas: audit log utk edit biasa (bukan cuma transisi status) | Selesai, di-extend lagi: nyimpen customer+produk yang diedit di snapshot, ditampilin lewat toggle "Lihat Detail" per entry. |
| Jenis PSSP dropdown per produk (PSSP/Retensi/Peremajaan/Perpanjangan) | Full-stack selesai, di-hide sementara dari UI (`{false && ...}` — logic backend tetap ada, gampang di-reenable). Kemungkinan terkait item #11 di atas. |
| Bug: teks error mentah "NEXT_REDIRECT" flash saat klik Simpan | **FIXED** — re-throw `isRedirectError()` sebelum ditangkep jadi error biasa. |
| Bug: Pengali Nilai R = 0 gak dikali 0 | **FIXED** — root cause "0 itu falsy di JS". |
| Case-insensitive login | Selesai — kemungkinan ini yang menjawab item #37. |
| Kalkulator estimasi quantity per produk | Selesai. |
| Tombol Delete draft POA di dashboard | Cuma muncul utk POA status DRAFT. |
| Tombol "+ Daftar User Baru" jadi toast "belum ready" | Gak navigasi lagi. |
| Card Estimasi Sales & Nilai PSSP side-by-side | Selesai. |
| Doctor/user combobox: label spesialisasi + nama dipanjangin | Selesai. |
| Rename "% PSSP Dokter" → "% PSSP User" | Termasuk header kolom Excel export tim. |
| PSSP Aktif ikut ke Excel export | Sheet baru "PSSP Aktif" di export per-POA dan export tim ASM/SM/NSM. |
| Verifikasi: ASM/SM/NSM hanya bisa edit, gak bisa bikin draft baru | Sudah benar dari awal, diverifikasi browser pakai 2 akun ASM real. |
| Placeholder deskriptif (Hari Praktek/Bln, Resep/Hari → Pasien Baru/Hari, Jml Produk ST/Resep) | Selesai. |
| Input Lama Periode dibuat lebih lebar | `LineItemEditor.tsx:434` — lebar 220px, sekarang lebih lebar dari field "Periode Awal" di sampingnya (200px). |

### ⬜ Belum

| Item | Catatan |
|---|---|
| Input Lama Periode dibuat nullable | ⚠️ Per cross-check kode 07-20: **belum** — kolom `lamaPeriode` di schema masih `Int` (`NOT NULL`, lihat migration `20260710020148_add_line_items_outlets`), dan UI di `LineItemEditor.tsx:435` masih pakai `<Req/>` + validasi wajib isi di submit handler (3 panel: Add/AddProduct/EditDoctor). Kalau memang mau dibuat nullable, ini perlu migration schema + hapus validasi required — bilang aja kalau mau saya kerjain. |
| Rawat inap vs rawat jalan | Label sudah diganti "Pasien Baru/Hari", logic pembeda formula rawat-inap-vs-jalan belum diimplementasi terpisah. |
| ConfirmDialog custom ganti native `confirm()` | Dipasang di tombol "✎ Edit". 2 `confirm()` lain (hapus baris produk, hapus dokter) belum diganti. |
